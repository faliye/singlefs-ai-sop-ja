C commit-publish-nofence
(* generated-from: templates/litmus/commit-publish-nofence.litmus sha256:36612e17783195285eacde277c48fc2aca78aeb9c6ee64935b0359cafde531f3 *)

(*
 * commit-publish の対照群：バリアを外したもの。
 *
 * 存在理由はただ一つ、**commit-publish.litmus に判別力があることを示す**ためである——
 * こちらも Never と判定されるなら、そのパターンは元々当たらないということであり、
 * commit-publish が Never でも何も示していないことになる。
 *
 * rules/show-me-test.md に従えば：赤くなることを示せない検査は書いていないのと同じ。
 * 対照群は、その規則をメモリ順序の上で形にしたものである。
 *
 * singlefs-expect: Sometimes
 *)

{}

P0(int *blk, int *gen)
{
	WRITE_ONCE(*blk, 1);
	WRITE_ONCE(*gen, 1);
}

P1(int *blk, int *gen)
{
	int r0;
	int r1;

	r0 = READ_ONCE(*gen);
	r1 = READ_ONCE(*blk);
}

exists (1:r0=1 /\ 1:r1=0)
