#!/usr/bin/env bash
# 样本：该绿的两种写法。判红就说明豁免条款被改坏了。
check_one() {
  bad "这里不合格"
  # 二、bad 与 howto 之间解释缘由的注释不占窗口名额——这里故意放 7 行，
  # 比窗口（5）还多，只有「注释不占名额」成立时才过得去。
  # 判据要落在内容上，不落在文件名上。
  # 文件名可以随便起。
  # 内容骗不了人。
  # 所以扫内容。
  # 下面这句才是出路。
  howto "照 templates/ 的样子补一份，再重跑。"
}
check_one

# 一、注释里举例写的拒绝不算一处拒绝。下面这段**故意不跟 howto**：
# 只有「注释里的 bad 不算」成立时，它才不会被要求出路。
#   写法甲： bad "不合格"
#   写法乙： cmd || { bad "不合格"; }
#   写法丙： if x; then bad "不合格"
# 到此为止，本文件再无别的内容。
