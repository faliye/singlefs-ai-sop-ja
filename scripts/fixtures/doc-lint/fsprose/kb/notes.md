# 正常内容

打开一个文件之后，该文件的 inode 号不会再变。
本文件系统的最小分配单元是 4 KiB。
旧数据已被新写入覆盖，读到的是新值。
这个代价已被两条不变量部分覆盖，剩下的那半还没有人管。
日志重放把文件系统还原为崩溃前的状态。
位图记录每个块是否曾经被分配过。
回收器扫描已废弃的 extent 并归还给分配器。
fsync 之前是未落盘的，fsync 返回之后才算持久。
压缩以前为 4 KiB，压缩以后为 1 KiB。
槽位表有 4096 项，该表常驻内存。
两次检索用相同上下文，命中率一致。
三条臂共同上报一份计数。
还原先前的镜像之后再跑一遍 checker。
链表的上一节点指针在崩溃后可能是悬空的。
三本文档说的是同一件事。

## 历史版本

### 2026-08-29
- 建档。
