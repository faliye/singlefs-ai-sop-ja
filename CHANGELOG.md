# CHANGELOG

規則とゲートのバージョン履歴。`CLAUDE.md` と `rules/*.md` は履歴節を持たない
（design-doc-discipline）ので、履歴はここに記す。個々の変更の詳細は `git log`
を参照——コミットメッセージがそのまま変更説明になっている。

## 0.0.50 — 2026-09-16

**一巡の総点検：`--staged` の経路にあった本物のバグ三つ、doc-lint が規則本体を一篇まるごと免除していたこと、0.0.48 以降の残りものを、まとめて直した。**

ゲート（`scripts/`、三リポジトリで同一）：
- `gate.sh --staged` の握手変数 `GATE_STAGED_FROM` が内側で起こす子プロセスに漏れていた：selftest が入れ子で走らせる gate.sh がそれを自分のプロジェクト根と見なして兄弟ディレクトリを探し、
  「上流が副本より古い」「副本が上流より古い」の二例が逆に赤になる——singlefs が 0.0.49 で `--staged` を走らせると必ず赤だった。利用者が指定した `GATE_BASE` も同様に selftest へ漏れ、
  show-me-test の二例が 3 で終わっていた。今は gate.sh が `GATE_STAGED_FROM` を読んだら即 `unset` し、selftest は子プロセスを起こすとき必ず `env -u` でこの二つを落とす。
- `--staged` は SOP リポジトリ自身では走らなかった：内側は源リポジトリのスクリプトを使うのに ROOT は一時ツリーで、利用側プロジェクトと見なされて「版のスタンプが無い」と赤になり、
  SOP リポジトリでしか走らない三段階は黙って走らなかった。今は源リポジトリがこのパッケージ自身なら一時ツリーの中の gate.sh を走らせ、i18n-sync は源リポジトリの親ディレクトリから兄弟言語リポジトリを探す。
- `--staged` の diff 基準が直接走らせるより一段狭かった：一時ツリーは detached HEAD で `@{upstream}` が解決できず、HEAD~1 に落ちる。
  「二回に分けてコミットすれば迂回できる」穴がこちら側では開いていた（実測：直接走らせると origin/master、`--staged` は HEAD~1）。今は基準を源リポジトリ側で算出してから渡す。
- shell-lint の S3 は `pgrep -f` に当たっても、同じ行に `xargs|kill|until|while|if` の部分文字列が無いと赤にしなかった：`pids=$(pgrep -f X)` の次の行で `kill $pids`、
  `while` と `pgrep -f` を二行に分けて書く、どちらも全緑で、`pgrep -f notify` は部分文字列 if を含むせいで待ちループと判定された。今はコマンド位置に現れれば赤。標本は `pgrepsplit`。
- 「版のスタンプが無い」は FAIL と記録しながら `warn` で印字し、対処も無かった。gate-lint は bad / die / ✗ しか見ないので、これが見えなかった。bad + howto に改めた。
- doc-lint の `rule-definition` の印はこれまで一篇まるごと免除していた：rules/ に翻訳調や「元は X だった」を入れても doc-lint は全緑で、同じ一文を README に置けばその場で赤だった。
  今は本文をそのまま見て、語彙リストと突き合わせる前にバッククォートと「」の中に挙げた例だけを抜く。規則 14 篇と CLAUDE.md が「スキップ」から「検査」になった（検査 14 → 29）。
  標本は rulesprose / ruleshiststate（赤）、rulesexample（緑）。
- doc-lint はインストール先の複製をワイルドカード `*/singlefs-ai-sop/*` と「ROOT がパッケージ内なら除外しない」という例外で除外していたため、
  走査範囲がパッケージの置き場所で変わっていた：同じ標本 projok が zh リポジトリでは「検査 1」、singlefs の複製内では「検査 2」となり、
  0.0.50 を同期した途端に向こうの selftest が一例赤くなった。ROOT 相対の `$ROOT/.claude/<family>/*` に改め、
  `/singlefs-ai-sop/` を含むパスにスクリプトを写して同じ標本を走らせる用例を追加。
- **プロジェクトのラッパー経由で呼ぶと `--staged` が黙って落ちていた**：ラッパーはプロジェクト根を `$1` に置き、
  利用者の引数をその後ろに付ける。一方 gate.sh は「`$1 == --staged`」と決め打ちしていたため、ルールと skill が勧める
  `bash .claude/scripts/gate.sh --staged` はあの分岐に一度も入らず、ゲートは作業ツリーを検査して何も言わなかった。引数は順不同になり、認識できない引数は拒否する。
- **heredoc の判定が heredoc でない三つの書き方を開始と見なしていた**（`<<<` の here-string、コメント内の `# … <<PY`、
  `$((1<<SHIFT))`）。その行以降ファイル全体が検査されなくなる——大きいスクリプトほど当たりやすい。
  区切り語の判定は `lib.sh` の `HEREDOC_RE` 一箇所に集約し（`CMD_POS` の先例）、コメント行は判定の前に飛ばす。
- **`… | grep -q` をパイプの末尾に置くこと**：grep は最初の一致で終了し、上流が SIGPIPE を受け、`pipefail` の下では
  パイプライン全体が 141 で終わるため条件が偽になる。パイプバッファ（64 KiB）を超えると検査が黙って走らなくなる
  （実測：件数を報じない同じスクリプトが 4 行では赤、338 KB では緑。テストを含む 108 KB の新規ファイルが「テストなし」と判定）。
  gate-lint の G3、show-me-test の二箇所、doc-lint の不変量番号の照合を `grep -c` に変更。
- **gate-ok が走り終えた時点の HEAD を記録していた**：別のセッションが実行中に積んだコミットが検証済みとして刻印され、
  以後すべての diff 窓から外れる。開始時点の HEAD を記録し、`GATE_BASE` が明示された回（`--staged` の内側を含む）は記録しない。
- **diff の基準が `@{upstream}` の tip で、merge-base ではなかった**：fetch だけして merge していないとき、上流の tip には手元にない
  コミットが載っており、作業ツリーがクリーンでも「crates のコードを変えたのにテストの変更がない」と判定された。
  偽の赤は人をゲートの迂回へ追いやる。
- **`GATE_BASE` を検証していなかった**：一文字の打ち間違い（`orgin/master`）で `changed_files` が空リポジトリ扱いになり、
  show-me-test は「判定対象なし」、ゲートは緑、gate-ok まで前進した。解決できない場合は対処付きで拒否する。
- **「ゲートの対象が SOP リポジトリ自身か」を `pwd` で比べていた**：シンボリックリンク越しに走らせると消費プロジェクトと
  判定され、バージョン印が無いとして赤、SOP 専用の三段階は黙って走らなかった。`pwd -P` に変更。
- **プロジェクト内で gate-lint / shell-lint を単独実行すると複製側の標本を報告していた**（わざと壊してある標本が
  プロジェクト自身の違反として出る）。両方とも `$ROOT/.claude/<family>/` を除外する（doc-lint と同じ形）。
- **kb の「## 履歴」見出しの行末に空白が一つあるだけで判定全体が空振りしていた**：本文走査がそこで止まらず、
  しかも「履歴の節で終わっていない」と報告された。行末の空白を落としてから比較する。
- **`x="$(sed -n … I18N)"` は I18N が無いと 2 で終わり**、`set -e` が出力ゼロのままスクリプトを落とす（gate.sh、gate-lint、shell-lint、doc-lint の二箇所、push-all の計六箇所。第四巡でさらに二箇所見つかった）。
- **pre-push が押す ref を見ていなかった**：フィーチャーブランチでもタグでも、`git push --dry-run` でさえ、
  他の二つの言語リポジトリが**実際に**プッシュされていた。今は master を押したときだけ起動する。
  フックの射程と、`--dry-run` という止められない罠は `CLAUDE.md` に明記した。
- **install.sh が置くファイルの権限が 0600 だった**（`mktemp` の権限が `mv` でそのまま残る）。644 に変更。
- `env.sh` にゲート自身が依存していながら一つも見ていなかったツールを追加：gawk、sha256sum、timeout、
  および git ≥ 2.28、bash ≥ 4.3、`sort -V`。`manifest.sh` の整列は `LC_ALL=C` に固定、selftest は利用者の
  グローバル / システムの git 設定から隔離する。

第三巡（前の二巡の報告で残っていた項目も 0.0.50 に入れた）：
- **shell-lint の S5 が一つの書き方しか認識していなかった**：`rm -rf "$d"/x`、`rm -rf -- "$d/x"`、同じ行の二つ目の引数にガードがない場合が、いずれも緑だった。二段階に改めた：まず `rm -r` を拾い、引数ごとにガードのない変数パスを探す。
- **git フックから `--staged` を走らせると失敗していた**：フックの GIT_DIR は相対パスで、gate.sh が作業ディレクトリを変えると別の場所を指し、worktree が作れない。しかも対処は「先に git worktree prune」だった。冒頭で GIT_* 一式を消し、作れないときは git の原文を出して `worktree remove --force` を示す。
- **`--staged` の後始末を EXIT トラップ一箇所に集約**し、どの経路で抜けても一時 worktree を消す。
- **「規範バージョン不一致」の拒否が出た瞬間に unbound variable になっていた**：族名の定義が数十行後ろにあった。使う前に移し、対処も .git のない複製で git log を走らせる案内をやめた。
- **ローカル段階が diff 基準をそれぞれの口径で取っていた**：gate.sh が一度だけ計算し、`GATE_DIFF_BASE` として各段階に渡す。
- **「判定対象なし」と「判定した」を分けた**：naming-lint は .rs がなければ 3 で終わり「今回未実行」に記録する。doc-lint に `--not-impl` を足し、語彙表のない言語ではその検査を PASS ではなく未実装一覧に載せる。
- **gate-lint が .py も走査する**。適用するのは「直接出力する ✗ には対処を付ける」の一項だけ（singlefs の lib-*.py にある 13 箇所は一度も見られていなかった）。
- **doc-lint の「廃止」パターン二つが同じメッセージを共有**しており、どちらを消しても標本は緑だった。分けて、それぞれに標本を付けた。
- **install.sh がバージョン印を下向きに書けた**：複製が印より古いのにそのまま書き、格下げがコミットに乗って全員に伝わる。今は拒否する。ラッパーは複製がないとき、bash の「No such file」だけでなく、どうすればよいかを言う。
- **i18n-sync が三箇所で `.git` をディレクトリとしてしか認めず**、worktree を拒んでいた。今回のリリースは worktree 上で作ったので、三箇所とも実測済み。
- **日付をでっち上げてはいけない**：標本三つと skill の例一つに 2026-01-01 とあり、このリポジトリの最初のコミットより半年以上早いのに、ゲートは緑のままだった。すべて実際の日付に直した：標本はそれぞれの作成日、skill の例は `date +%F`。doc-lint は履歴項目と「実測（日付）」を、changelog-lint は版の節の日付を、selftest は標本の日付を検査する。下限は検査対象プロジェクト自身の最初のコミットから 7 日の猶予をさかのぼった日（そのディレクトリ自体がリポジトリの最上位で、しかもシャロークローンでないときだけ git に聞き、外側のリポジトリまで遡らない）、取れなければ本パッケージの起点 2026-08-26。上限は時刻が最も進んでいるタイムゾーン（UTC+14）での今日。両端をなぜこう定めたかは第四巡を参照。
- gate.sh の判定分岐に用例を追加した（ローカル段階を二つの lint に渡す、Show me test の 3、インストールした複製でない方を走らせた、.rs があるのに Cargo.toml がない、`--staged` で無視された複製をコピーする）。これらは PASS にすり替えても用例が一つも赤くならなかった。bump.sh と i18n-sync `--stamp` の拒否分岐はそれまで用例ゼロで、今回補った。
- lib.sh から誰も呼ばない `added_lines` を削除。

第四巡（三つのモデルが順推・逆推・照合に分かれてこの版を二巡審査し、見つかった問題もまとめて直した）：
- **日付の検査が本物の日付をありえないと判定していた**。上限は本機の時計の日付を取っていたが、本機の時計は UTC である：東京で 00:00–09:00 に書いたその日の日付が「今日より後」と判定され、
  singlefs の 194 コミットのうち 8 つがこの時間帯に、コミット時の UTC 日付より後の日付の履歴項目を書いていた。下限は最初のコミットに固定されていたが、git init より前にやった作業はそのときの日付を持ったまま最初のコミットに入る：
  singlefs の初版コミットには前日の履歴項目が 5 つあり、移管前の 0.0.50 で走らせると、そのゲートはこの 5 箇所で赤だった（審査での実測）。
  今は上限を時刻が最も進んでいるタイムゾーンで算出し、下限は 7 日の猶予をさかのぼる（実測では一日早いだけだった）。2026-01-01 のようなものは変わらず止められる。4 つの用例が両端を釘付けにし、時計は偽の `date` で固定する。
- **退役ラッパーを名指しする仕組みを QEMU と一緒に削除していた**が、同じ版で新しい退役ラッパーが生まれていた：singlefs の `.claude/scripts/lkmm.sh` は削除済みのスクリプトへ転送し、
  install.sh はいつもどおりバージョン印を更新し、それを走らせて得られる対処は「副本が入っていない」だった。仕組みを戻して汎用のものにした。後述の移管の節を参照。
- **未実装一覧が受入基準に触れなくなっていた**：QEMU の二つのキーを削ったあと、プロジェクトが最終判定基準の段階を一つもつないでいなくても、集約は一言も触れなかった。装置を名指ししない「最终判据」（最終判定基準）に置き換えた。後述。
- **I18N が無いと出力ゼロになる箇所がまだ二つ残っていた**：doc-lint が `this=` を読むところと、manifest が `this=` と `reference=` を読むところ。`|| true` を補い、それぞれに用例を一つ付けた。
- **selftest が訳本の標本を組むとき、スタンプ付けの出力を `/dev/null` に捨てていた**：スタンプ付けと読み戻しの形が食い違うと、lib.sh の `set -e` が自己検査全体を第 245 例で道連れにし、
  残りの 58 例は一つも走らず、終了コード 1 だけが残った。今は失敗したら原文を出してから止まる。
- **仮想機と関係のない内容まで誤って削っていたので、戻した**：`command-safety.md` の「テストイメージは必ず一時ディレクトリに置く」「破壊的な操作は、動かす前に目で確かめる」の二節と、表の `mkfs`、`dd`、`lsblk`。
  クラッシュ点リプレイもイメージと mkfs を要し、singlefs のコードとスクリプトも「テストイメージは必ず一時ディレクトリに置く」を見出しで引いている。`env.sh` の dmsetup が見ているのはクラッシュ点リプレイのブロック層の書き込み記録なので、これも戻した。
- 文面：`show-me-test.md` に取り込んだ「スキップ一覧」の段落は、gate-lint がもう `noskip` を検査しているかのように書いていたが、実際にはその検査は無いので、まだ検査になっていないと明記した。
  doc-lint のファイル冒頭のコメントが、まだ rule-definition は一篇まるごと飛ばすと言っていた。pre-push のコメントは `--dry-run` の罠が README に書いてあると言っていたが、実際は `CLAUDE.md` にある。
  `test-discipline.md` の「不可推翻」二箇所は、前の巡で一箇所しか直していなかった。日付の項目に書いた「標本五つ」は実際には三つで、fence 標本の 2026-01-01 と書いてあった箇所はその作成日に改めた。
  env.sh の「sort が -V を認めない」の例は再検証できる書き方に改めた（0.0.9 → 0.0.10 が格下げと判定される。審査での実測）。
- 審査の二巡目でさらに見つかったもの：
  - **マシンの date が `-d` を認めないと、日付の下限が黙って消えていた**。2026-01-01 もそのまま通っていた：日付の検査は `if` の条件の中で呼ばれており、`set -e` は効かない。
    doc-lint と changelog-lint は冒頭で一度 `date -d` を試し、使えなければ止まる。env.sh もこの項目を見る。
  - **シャロークローンの「最初のコミット」は切り詰められた地点である**。それを下限にすると、プロジェクトが古いほど誤判定が増える。シャロークローンでは git に聞かず、本パッケージの起点に退く。
  - **en / ja リポジトリの「Measured (日付」「実測（日付」は一つも検査されていなかった**：中国語の「实测（」しか認めておらず、検査されていないこの半分は未実装一覧にも載っていなかった。三つの書き方をすべて認めるようにした。
  - **退役ラッパーは現行の形しか見分けていなかった**：0.0.49 以前に敷いた三行のラッパーは見分けられず、いつもどおりバージョン印を更新していた。両方の形を見分ける。プロジェクトがまだある共有スクリプトをテンプレートどおりに包んだラッパーは退役扱いしない。
  - **猶予 30 日では、でっち上げた日付を通しすぎる**：実測では一日早いだけだったので、7 日に縮めた。
  - **「作業ツリーの指紋は本物のインデックスに触れない」を見る用例が selftest に一つもなかった**：指紋を本物のインデックスの上で直接 `git add -A` する形に変えても、算出されるツリーはバイト単位で同じで、既存の用例は一つも赤くならなかった。
    しかも利用者がゲートを走らせるたびに、ステージしていない変更が黙ってステージされる。用例を一つ補い、呼び出しの前後の `git status` を直接比べる。
  - 文面：取り込んだ「接続詞は接続詞として書く」の節は `⇒` を使わないよう求めているのに、この版で新しく書いた規則の行に `⇒` が 7 箇所あった。接続詞に改め、ゲートはこれを見ないこと、既存の文はまだ洗い直していないことを明記した。
    `show-me-test.md` の「スキップ一覧」の一文は、実装されていない免除の書き方をもう書かない。「最终判据」は、まず受入基準をプロジェクトの kb に書くこと、一部しか満たさない段階はこのキーを書かないことを明記した。
    モデル対照テストまで記録ストリームを要するかのように書いていた三箇所の文を正した。`session-wrapup.md` のあの実測は、踏んだのがパスを付けないコミットのほうだと明記した。
    gate skill の段階表に「実行中に作業ツリーが変わらなかったか」を補った。gate.sh で重なっていた二つの節見出しを分けた。doc-lint と changelog-lint の数箇所のコメントを猶予に合わせた。
- 赤くなる証拠：日付の上限、猶予、外側のリポジトリまで遡らないこと、退役ラッパー、まだある共有スクリプトをテンプレートどおりに包んだものは退役扱いしないこと、I18N の二箇所、最終判定基準——審査の一巡目で見つかったこの 8 つの修正に、
  二巡目の `date -d`、シャロークローン、英日の実測の書き方、旧い形のラッパー、猶予 7 日、指紋が本物のインデックスに触れないこと、の 6 つを加え、一つずつ旧い形に戻すと、そのたびに狙った用例が赤くなった。
  スタンプ付けが失敗したときに原文を出す箇所には専用の用例がない：スタンプ付けの読み戻しを壊し、自己検査が最初に標本を組むところで止まって原文を出すのを見て確かめた。

同じ時間帯に別のセッションが行った変更も、この版にまとめて取り込んだ：
- **ゲートの実行中に作業ツリーが変わったら赤にする**：gate.sh は開始時と終了時に作業ツリーの指紋を一度ずつ取り（追跡中のファイルと、無視されていない未追跡ファイル。写し取った一時インデックスで算出し、本物のインデックスには触れない）、
  食い違えば赤にする。対処は、編集が止まってから走らせるか `--staged` を使うこと。selftest に二例：途中でファイルを変えたら赤、途中でステージしただけなら変化に数えない。
  「gate-ok は開始時の HEAD を記録する」の用例は、走らせる前に内容を書いておき、実行中は HEAD だけを動かす形に改めた。
- `show-me-test.md` に二段落：何件検査したかを報じるだけでは足りず、検査しなかったものも一件ずつ挙げ、その一覧はその場で算出する。成功行で報じる数も標本で釘付けにする。
- `evidence-discipline.md` に二段落：結論に入る数は、まず測ったのか見積もったのかを問う。撤回のときに洗い出した一覧は一括で置き換えず、現状を述べているのか、その時に起きたことを述べているのかを先に見分ける。
- `writing-discipline.md` に一節：接続詞は接続詞として書き、名詞の羅列は文に戻す。
- `session-wrapup.md` に二項目：`--staged` なしで走らせている間に作業ツリーが変わると赤になる。「このパスだけをコミットする」は `git commit -- <パス>` ではない。

selftest は 268 → 320 例：三巡の修正で 58 例増え、第四巡で 12 例増え、別のセッションの変更で 2 例入り、QEMU と herd7 の移管で 20 例減った（後述）。赤くなる証拠：三巡の修正を一つずつ旧い形に戻し、そのたびに狙った用例が赤くなった。
うち四つの用例は初版が偽物で、変異させても黙っていた：大きいファイルの標本の埋め草をコメントで書いた、`GIT_DIR` を絶対パスで書いた、die の前に手で後始末をもう一度していた、ローカル段階が自分で赤くなって lint の判定を覆い隠していた。変異のうち二回は case 文の構文を壊していたので、行ごと置き換える形でやり直した。いずれも赤くなるまで直した。

規則と文書（各言語とも一緒に改めた）：
- `command-safety.md`：赤くなる検査は四条ではなく五条、`pgrep -f` を補った。S3 の記述をスクリプトと一致させた（コマンド位置なら一律赤）。「基于」一箇所。
- `sop-first.md`：拒否の形態「二つ」→ 三つ。「17 箇所の die がまとめて検査を免れていた」「これまで全て検査を免れており」の二つの履歴叙述は削るか実測記録に改めた。
- `show-me-test.md`：ローカル段階の「従来はどちらの走査範囲にも入っておらず」を実測記録に改めた。
- `test-discipline.md`：「不可推翻」二箇所を節見出しと同じ「不可证伪」（反証不可能）に揃えた。「本節の主結論」は名指しに改めた。
- `verify-before-claiming.md`：節見出し「它定了没，和它到底怎么定义的，是两个问题」の内側の引用符を外し、二箇所の引用と一致させた。
- `kb-discipline.md`：「本文件」はゲートが見ず人が見る、と明記した。`design-doc-discipline.md`：doc-lint が見るのは `.md` だけで、コードコメントの条はレビューに頼る。
- `engineering-philosophy.md`：howto 要件があるのは `sop-first.md` であって `show-me-test.md` ではない。判定基準の一文は `machine-first.md` の原文を引く形に改めた。
  `machine-first.md`：`engineering-philosophy.md` と逐語で重複していた段落を、一文と参照に縮めた。
- `code-discipline.md`：処置ラベル三箇所（「この理由は廃止」「残す、しかもより強く」「型に任せる」）を、宣言した六種類に収めた。
- `writing-discipline.md`：「ゲートが見るのはどちら半分か」に、規則本体も見ること・免除されるのは例だけであることを補った。
- skills：`gate` に `--staged` の使い方を補い、ゲート自己検査と shell 規律の失敗理由を全部列挙し、en / ja 副本では規則マニフェストが該当しないこと、rule-definition の新しい意味を書いた。
  `crash-test` の gate-covers キーに「命名纪律（shell）」を補った。`decide` の「本工程」→「本项目」。
- templates：kb 三篇が規則を指す相対パスを、`.claude/kb/` から解決できる `../singlefs-ai-sop/rules/…` に改めた。プロジェクトテンプレートの shell-lint のコメントを全部列挙した。
- README：「克隆」→「副本」。三つの関門の段落で「gate-lint が強制する」と「人が判断する」の二文が食い違っていたのを、ゲートが見るのは第 3 関門の形式だけ、に改めた。
- en / ja：0.0.48 / 0.0.49 の訳文にあった用語の不統一をついでに直した（英語は remedy を「出路」「改法」の両方に訳していた。日本語は「写死的 pid」「虚机」がそれぞれ二通りの書き方だった）。
- 第三巡：`test-discipline.md` と `verify-before-claiming.md` に「ゲートが見るのはどちら半分か」を足し、検査になっているものと人に頼るものを明記。`command-safety.md` に「プロセス境界で起きる三つの沈黙した失敗」を追加。`code-discipline.md` に、shell の命名規律が既存スクリプトへまだ遡って適用されていないことを明記。`sop-first.md` から数を含む記述を外した。英日それぞれ二通りに分かれていたその節見出しの訳を統一。

QEMU と herd7 はまるごと singlefs に移管した。使っているのは singlefs だけなので、どうテストし、どう検証し、ゲートに組み込むかどうかは singlefs 自身が決める。本パッケージはもうテストも検証もしない：
- `scripts/lkmm.sh`、`scripts/fetch-deps.sh`、`scripts/fixtures/lkmm/`、`templates/litmus/` を削除。ゲートに LKMM 段階はもうない。
- 未実装一覧から「QEMU 真实负载」「QEMU 崩溃注入」の二つのキーを外し、装置を名指ししない「最终判据」（最終判定基準）に置き換えた：受入基準はプロジェクトが決める。
  プロジェクトの段階がそれを満たし、`# gate-covers: 最终判据` と書き、この回に走って通ったときだけ、この項目は「どの段階が覆うか」に変わる。
- `install.sh` は `.claude/scripts/lkmm.sh` を敷かず、プロジェクト直下に `litmus/` も置かない。退役ラッパーの名指しは汎用にした：`.claude/scripts/` にあってラッパーのテンプレート（現行または 0.0.49 以前の形）と一字違わず、
  しかも転送先の共有スクリプトがこの版に無いものは、すべて名指ししてバージョン印の更新を止める。決め打ちの `qemu.sh` だけを見るのはやめた。ラッパーの全文は `wrapper_text` 一箇所にだけ書き、新しいラッパーを敷くときも退役ラッパーを見分けるときも同じ文面と比べる。
- `env.sh` は qemu-system-x86_64、fio、`/dev/kvm` をもう見ない。
- `i18n-sync.sh` から `.litmus` 用の出所マーカーの形を削除し、`install.sh` もマーカーを剥がすときにその形を認めなくなった。`manifest.sh` の訳文一覧とカバレッジ検査は `.litmus` を数えない。
  `show-me-test.sh` の対処から、並行とメモリ順序の二行を削除。
- 規則：`show-me-test.md` の「最終的な判定基準は QEMU/KVM の負荷試験である」を「最終的な判定基準はプロジェクトが決める」に改めた。`machine-first.md` の前提二は herd7 / LKMM を名指しせず、
  どのツールを使うか、その判定が差を見分けられることをどう示すか、どうコードに結びつけるかはプロジェクトが決める、と明記し、根拠は singlefs の kb を指す。表の二種類の「モデル」は書き分け、網羅される側を「形式モデル」と呼ぶ。
  `command-safety.md` から「QEMU の仮想機は pid をファイルに書くこと」の節を削除した。プロセスの終了を待つ一文の「仮想機など」は、自分で起こしていないプロセスに改めた。
  結果の回収の一文の「BIOS のエスケープシーケンス、カーネルログ、シリアルのノイズ」は名指ししない書き方に改めた。サブシェル代入の実測記録は QEMU harness を名指ししない形にした。
  `test-discipline.md`、`code-discipline.md`、`sop-first.md` もそれに合わせた。
- skills：`crash-test` から LKMM と QEMU の二節を削り、gate-covers のキーは `模型对拍`（モデル対照テスト）、`崩溃点重放`（クラッシュ点リプレイ）、`最终判据`（最終判定基準）、`命名纪律（shell）`（shell の命名規律）になった。`gate` から LKMM の行を削除。
  プロジェクトテンプレートと README もそれに合わせた。GLOSSARY の「対照群」は litmus に即して定義していたが、`test-discipline.md` と一致する上位概念に改めた：陽性対照か実ベースライン。
- selftest から lkmm の標本と litmus の出所マーカーの二組、計 20 例を削除。退役ラッパーの 3 例は lkmm.sh で作る形に改めた。shell-lint の標本にあった `stop_vm`、`HAS_KVM`、`KERNEL_PATH` のような名前は中立な名前にした。
- 削除前の原文は singlefs の `.claude/handover/qemu-herd7/` に置いてある。
- **この版はバージョン番号を上げないので、規則が変わったことをゲートは知らせない**：singlefs のバージョン印はもともと 0.0.50 で、「規範バージョン」「副本と上流が同じ版」の二段階はそのまま緑になる。同期は全面的に人頼みである。
  同期するとき install.sh が二箇所で止める：`.claude/install-owned` の litmus 二行（本パッケージはもうその二つのパスを敷かない）、`.claude/scripts/lkmm.sh` ラッパー（転送先が削除済み）。
  先に前者で止まり、直してから走らせ直して初めて後者が見える。ゲートは一箇所で止める：`gate.d/55` 冒頭の `# gate-covers: QEMU 真实负载`。このキーはもう一覧にない。
  55 番の冒頭に書いてあるとおり、その段階は実負荷と、必ず赤と判定されなければならない対照二つを走らせるが、クラッシュ注入はない。だからこの行は削る。受入基準を kb に書き、段階をすべて満たしてから `最终判据` と書く。
  `litmus/` を判定する共有段階はもうない。判定を続けるなら、移管した `lkmm.sh` をプロジェクト自身の段階として組み込む。そのほか、赤くはならないが同期後には真でなくなる箇所は、移管ディレクトリの README に挙げてある。

手を付けなかったもの：`evidence-discipline.md` の「五箇所見つかった……」の括弧内は四箇所しか数えられないが、原記録は singlefs にあり、ここでは確かめられない。GLOSSARY にあるリポジトリ全体で未使用の用語七つは、確認待ち。
changelog-lint と doc-lint の標本には、標本がリポジトリに入った日より早い日付がまだいくつもある（例えば 2026-09-10 に入った標本に 2026-09-01 と書いてある）が、どれも上下限の内側にある。
それらは標本の中の架空の版の節と履歴項目であって、2026-01-01 のような仮置きの日付ではない。この版では直しておらず、直すかどうかは未定。

意図的に手を付けなかったものがあと二つある。`evidence-discipline.md` の「ゲートが見るのはどちら半分か」は足していない：この版には別のセッションがその一篇に加えた二段落の変更を取り込んでおり、この節を足すにはもう一巡の審査が要る。規則の事例段落の削減と、重複した判定基準の統合はしていない：削るのは証拠そのものであり、迷うものは残す。

## 0.0.49 — 2026-09-14

**0.0.48 の締めくくりで見つかったゲートの穴四つを、まとめて塞いだ。**

- `gate.sh --staged` は内側のゲートが赤になると一時 worktree を残していた：外側が「内側; rc=$?」と書かれ、
  lib.sh が `set -e` を立てているので、内側が赤になると外側はその行で抜け、片付けまで届かない——赤のときこそ結果を見たいのに。
  実測：新しいリポジトリで、内側が通ると worktree の登録は 1 → 1、赤だと 1 → 2。終了コードは `if` の中で取るようにした。
- `project_root` は `.git` がディレクトリのときしか認めず、git worktree の `.git` はファイルである。九つのスクリプトが
  `${1:-$(project_root)}` と書いているので、worktree の中で引数なしに走らせると `/` まで遡り、`set -e` の下で何も出さずに
  1 で終わっていた。`.git` がファイルでも認め、根が本当に見つからないときはそう言って対処を示すようにした。
- 「副本と上流が同じ版」は、副本のほうが新しいときも「副本が上流より古い」と報じ、対処が逆向きだった
  （副本を複写し直すと古い版が戻ってくる）。向きで分けた：上流が古ければ「上流が副本より古い」と報じ、対処は上流の更新である。
- `install.sh` はプロジェクトが消したテンプレートを敷き直していた（`put` は既存ファイルを上書きしないだけ）。
  install-owned に登録済みでプロジェクトが消したものは、もう敷き直さない。

selftest は 267 例（+7：--staged の赤での片付け、worktree の中での根の検出、根が無いときに言うこと、版の二つの向き、
登録済みで消したものを敷き直さないこととその読み返し）。五つの修正を一つずつ旧い書き方に戻すと、どれもそのために書いた用例で赤になる。

別のセッションが中国語リポジトリの作業ツリーに残したまま提交していなかった実測の教訓二つも、この版に取り込んだ
（版上げ・マニフェスト更新・翻訳のどれもされていなかった）：
- `command-safety.md`：待ちループの中の `pgrep -f` はループ自身のコマンドラインに当たり、永遠に抜けない。プロセスの終了は
  リテラルの pid（`kill -0`）か `wait` で待つ。`shell-lint.sh` の S3 は `if` / `while` / `until` / `!` の後ろのコマンド位置まで広げた
  （`lib.sh` の CMD_POS がこの一か所だけの定義）。標本は `pgrepwait`。
- `evidence-discipline.md` の「判定基準そのものも書き誤ることがある」に四つ目の形を加えた：走らせる前の条項が示す直し方が、
  当たったマスに届かない。当たったあとにまず問うことが三つから四つになった。

selftest はこのぶん一例増えて、計 268 例。

## 0.0.48 — 2026-09-14

**singlefs の `crates/` にコードができたので、LKMM と QEMU の二つのゲートをそれに合わせて改めた。**

LKMM（`scripts/lkmm.sh`）：
- **対照群を中身で見分ける**。ファイル名だけでは見ない：コメントと一行目を除いたうえで、対照群に許されるのは
  Never の一本からバリアの行を抜くこと、または `smp_store_release` / `smp_load_acquire` を `WRITE_ONCE` /
  `READ_ONCE` に緩めることだけで、少なくとも一か所。ファイル名だけで見ていたときは、exists や読み手を変えた
  「対照群」でも関門を通り、接頭辞がぶつかると（`a` と `a-b`）`a-b` の対照群が `a` のものとしても数えられた。
- **Never の一本ごとにコードへ結びつける**：冒頭に `singlefs-models: <パス>::<関数名>` を書き、ゲートはファイルが
  在ること、`fn` が在ること、`crates/` のどこかの `.rs` がこの litmus のファイル名を書いていることを確かめる。
  どのコードにも対応しないものは `none —— <理由>` と書く。herd7 は litmus に書かれた形しか判定しないので、
  コードが順序を変えても litmus が追いつかなければ判定は Never のままだった。テンプレートの
  `commit-publish.litmus` は `none` にした。
- `--static-only` を新設：herd7 の要らない検査だけを走らせ、全部通っても 3 で終わる。selftest は標本をこれで流すので、
  標本の判定が機械に herd7 があるかどうかで変わらなくなった。

QEMU：
- **共有の `scripts/qemu/run.sh` と、ゲート段階「QEMU harness 自己検査」（`GATE_QEMU`）を削除した。**
  これはディスクを繋がず、shell スクリプトしか受け取らず、結果も取り込まなかった。そのため singlefs は
  仮想機の装置を自前で書き、ゲート 55 はそちらを使っている——共有側の自己検査は、誰も使っていないものを
  証明していた。仮想機の装置はプロジェクトに属し、装置が守る規則は引き続き `rules/command-safety.md` にある。
- `install.sh` は `.claude/scripts/qemu.sh` を置かなくなった。プロジェクトに手つかずの古いラッパーが残っていれば
  名指しし、消すまでバージョンスタンプを更新しない。

`gate.sh`：
- **未実装一覧はプロジェクトの宣言で覆う**：ローカル段階の冒頭に `# gate-covers: <項目>` と書き、その段階が
  この回に走って通ったときだけ、その項目は「どの段階が覆うか」に変わる。一覧に無い項目を書けば赤。
  一覧から「QEMU クラッシュ注入」（受入の最終判定基準）を「QEMU 実負荷」と分けた。以前は一覧が固定で、
  singlefs はゲートのたびにクラッシュ点リプレイと実デバイスの段階を走らせていたのに、集約は
  「被検対象が無い」「クラッシュ一貫性はまだゲートに入っていない」と印字し続けていた。
- ローカル段階が終了コード 77 を返したら「今回は走らず」と記録し、通過にも覆いにも数えない。

規則も合わせて改めた：`show-me-test.md`（仮想機の装置はプロジェクトに属する、覆いの宣言、終了コード 77）、
`machine-first.md`（対照群は中身で見る、litmus はコードに結びつける）。skill `crash-test`・`gate` と
プロジェクトのテンプレートも追従した。`gate` skill には、中国語原文が初版から持っていてこの訳文に欠けていた
「未実装の段階」の節も入れた。

## 0.0.47 — 2026-09-13

**0.0.46 の三言語一致確認。訳文の問題 4 件と中国語原文自体のずれ 1 件を修正した。**
四つのエージェントがそれぞれ `evidence-discipline.md`、`show-me-test.md`、`test-discipline.md`、
`verify-before-claiming.md` の中日英三言語を段落ごとに意味の一致を確認した（show-me-test は
問題なし）。修正内容：`verify-before-claiming.md` の英語版で強調が一つ抜けていた
（「それが何を定めているかを知っていることにはならない」）；同じ文書の「他人からの事実の訂正」が
英語・日本語ともに「the user」「利用者」に狭められており、`pushback-discipline.md` の同一の中国語文
の訳（someone else / 他人）と食い違っていたので揃えた；`test-discipline.md` の英語版で「作废条款」
（discard clause）と「失败条款」（failure clause）という別の二つの用語が一箇所で混同されていたので
`discard clause` に戻した；`evidence-discipline.md` の日本語版で「门禁」が「門番」と「ゲート」の
二通りに訳されていたので「ゲート」に統一した；英語版の「判据」が一箇所「Test」とずれていたので
「Criterion」に戻した。あわせて中国語原文自体のずれも見つかった：自問表がこれまでの追加で 6 行に
なっていたのに、本文はまだ「这四条」（これら四条）のままだったので「这六条」（これら六条）に直した。

## 0.0.46 — 2026-09-13

**2026-09-13 に実測した七つの教訓を四つの規則に足した。ゲートは変えていない。** `evidence-discipline.md`：
「先に結論を決めてからモデルを組んではならない」の自問表に一行（この数は掃いていないどれかのパラメータの関数ではないか）；
「腕の定義も『走らせる前に固定する』対象」に一段——産物を走らせる前に腕や判定基準を改めるのは正当だが、走らせる前の登録に
何を変えたか・根拠はどの単体テストの読みか・時点は産物より前であることを書く；新節「判定基準そのものも書き誤ることがある：
当たったら、まずどの形かを判じる」で三つの形（当たりが腕を区別しない、判別に使う量が観測できない、当たりを別の基準に分類している）と、
当たったらまず問う三つを挙げた。`test-discipline.md`：失敗条項の節の下に小節「判定基準を連言で書かない。閾値を被検アームの定義の
同語反復にしない」、新節「端点は軌跡ではない：条項が述語の入力にする量は、実験で軌跡を報じる」。`show-me-test.md`：装置をまたぐ検査も
値だけを釘付けにして量を釘付けにしていないことがある——量が二つなら名前を二つ登録し、それぞれの値を釘付けにする。
`verify-before-claiming.md`：切り詰めた出力も「狭いほう」である。「ここだけ」と言うなら出力を切り詰めず、長ければ先に数える。

## 0.0.45 — 2026-09-11

**`gate.sh --staged` は途中で割り込まれても一時的な worktree を片付ける。** これまでは Ctrl-C で止めると worktree が
リポジトリに登録されたまま残り、次は手で `git worktree prune` する必要があった。worktree を作った後に INT / TERM の trap を張り、
片付けてから終わるようにした。selftest に一例を加えた：20 秒眠るプロジェクトの段に途中でグループごと INT を送り、
その後リポジトリに登録された worktree がちょうど一つであること；trap を外すとこの例は赤くなる。

## 0.0.44 — 2026-09-11

**`gate.sh --staged`：「HEAD＋ステージ済みの変更」だけでゲート全体を走らせる。** 複数のセッションが同じリポジトリで作業するとき、
作業ツリーには他のセッションの作りかけの変更や未追跡のファイルが混ざり、作業ツリーで赤くなったゲートは誰の赤かを言えない——
`session-wrapup.md` の第 4 項はこれまでファイルごとに突き合わせさせるしかなかった。一時的な worktree で `git diff --cached` を
HEAD に当ててからゲート全体を走らせる：他のセッションの未コミットの変更と未追跡のファイルは入らず、赤くなったものがこのコミットの
持ち込んだものである。git に入っていない規範のコピーはそのまま worktree に写し、兄弟ディレクトリの上流は元のプロジェクトルートから探す。
きっかけは singlefs の 2026-09-11 の締め：作業ツリーのゲートが五つの段で赤くなり、そのうち三つは別のセッションのもので、
手で worktree を組んでようやく見分けた。selftest に三例を加えた：ステージしていない違反は `--staged` では数えない；
`--staged` なしでは同じ違反を数える（その段が本来赤くなることの証明——この例がないと、`--staged` の緑が「入れなかった」のか
「そもそも赤くならない」のかを区別できない）；ステージした違反は数える。`session-wrapup.md` 第 4 項の二つ目に使い方を一文加えた。

## 0.0.43 — 2026-09-11

**`i18n-sync.sh --update` が訳本リポジトリの `SOURCE-MANIFEST.sha256` を自分で更新する。手で写すことはもうない。**
0.0.42 を出すとき、en / ja の SOURCE-MANIFEST が古いマニフェストのままだったため `--update` は「遅れている」として丸ごと拒まれ、
共有部分が一つも同期されなかった——その一歩はこれまで、zh の `MANIFEST.sha256` を写すのを誰かが覚えていることに頼っていた。
今は `--update` が SOURCE-MANIFEST の遅れや欠落に出会うと、まず訳文すべての由来の印を一編ずつ確かめる：すべて現在の原文と合えば
このリポジトリのマニフェストから更新して読み戻し、一編でも欠けている・印がない・古い原文の訳であれば写さず、その編を挙げる。
一編ずつの判定は一つの関数 `stale_articles` にまとめ、この一歩と既存の第 5 項の照合が共用する。手で写すよう教えていた二か所の
「どうするか」は `--update` を走らせるよう改め、三つの README も同じ言い方に揃えた。`selftest.sh` に 5 件の用例を追加：
訳し直して印を押せば自分で写し、写しはマニフェストとバイト単位で一致する。訳し直していなければ写さず、SOURCE-MANIFEST は
一バイトも変わらない。欠けていれば作る。

## 0.0.42 — 2026-09-11

**プッシュをすべての言語リポジトリで一緒に検証し、一緒にプッシュするようにした。** 0.0.41 は zh だけがプッシュされ、
en / ja はそれぞれ二コミット遅れていた。リモート上では三つの言語が別々の版の規則を述べていたのに、ゲートはローカルでしか動かず
リモートが見えないので、何も警告しなかった。`scripts/push-all.sh` と git フック `scripts/githooks/pre-push` を追加：
各言語リポジトリで一度ずつ `core.hooksPath` を設定すると、以後どのリポジトリをプッシュしても、まずすべての言語リポジトリを検証し
（master 上、作業ツリーがクリーン、VERSION が同じ、それぞれのゲートが緑）、すべて通ったら他の言語リポジトリを先にプッシュし、
それから自分のプッシュを通す。一つでも通らなければ一つもプッシュしない。途中で拒否されたら、すでにプッシュしたのがどれかを報告する。
git worktree からプッシュすると、git はフックに自リポジトリの絶対パスの `GIT_DIR` を渡し、それが `git -C` より優先されるので、
スクリプトは最初にそれを消す——消さないと、他の言語リポジトリの検証とプッシュが実際には自分に向かう（通常のクローンからのプッシュでは、
フックにこの変数は来ない）。`selftest.sh` に 10 件の用例を追加し、
ローカルのベアリポジトリをリモートにして、本物の `git push` → フックの経路を通す。`CLAUDE.md` に有効にする方法を一文足した。

**規則を四条補った。singlefs で今回踏んだ落とし穴から。** `command-safety.md` に「ゲート付きのスクリプトは、判定し終えてから産物を渡す」を追加：
先に出力して後で判定すると、呼び出し側のリダイレクト先ファイルに無効と判定された産物が残り、正当なものと見分けがつかない。
`evidence-discipline.md` に二条：腕の定義で「どうやるか」から「やるとどうなるか」が導けなければ、別々の文で判定する各脚は別々の腕を判定している。
走査で得た極値が区間の端点に落ちたら測った数ではなく、「≤ 端点」としか書けない。`session-wrapup.md` の第 4 条は前提を三つから四つにした：
新しく作ったファイルが別のセッションの書いたばかりの未コミットのファイルを上書きしうるので、新規作成は排他的に行う。

## 0.0.41 — 2026-09-10

**`rules/code-discipline.md` を追加：machine-first の実施細則。** 原則は一つだけ：過去のベストプラクティスに代わりに決めさせない。
`machine-first.md` は旧来の規則をなぜ吟味し直すのか、どう吟味するのかを述べていたが、吟味した後にコードをどう書くかは書いていなかった。
命名の一条（名前の長さに上限を設けない、略語を使わない）は `engineering-philosophy.md` にあり、レビューで見る一つの言い方であって、規則ではなかった。
新しい一編はそれらを細則に落とす：名前、分岐、型、エラー、関数とネスト、コメント。加えて出典ごとにまとめた表——『Clean Code』、SOLID、
『リファクタリング』、『達人プログラマー』、Rust API ガイドライン、Linux カーネルのコーディングスタイルなどの流行りのやり方 85 条を 10 組に分け、
一条ごとに処置とこちらの書き方を与えた。コードのレベルの吟味結果は他の二編から移し、その二編には理念と、設計・工程レベルの表だけを残した。
移した例の中の略語の名前（`dev`、`m`、`Lba`、`commit_txn`、`num_`）も書き切った。

同じ版で、レビューの指摘に従い六箇所の言い方を引き締めた：経路数が測るのは制御フローだけで検証の難しさのすべてではなく、
ループの反復上限、回をまたぐ状態、早期脱出は別途明記すること。ワイルドカードアームを禁じる対象は閉じた集合であり、
意味上未知の値を許すものは「未知」を明示的な列挙子にして `match` をそのまま網羅すること。「一つの概念に一つの名前」の単位は意味概念であって語ではないこと。
名前をモジュールパスで補わないのは、意図して名前に名前空間の情報を担わせる選択だと明記すること。`as_` / `to_` / `into_` は規約であって保証ではなく、
C-CONV の表を基準とすること。エラーの列挙子は呼び出し側の判断ごとに分け、下層の原因ごとには展開しないこと。
`engineering-philosophy.md` と `machine-first.md` の「経路数が有界なら検証できる」も、それに合わせて必要条件に改めた。

**ゲート段階「命名規律」（`scripts/naming-lint.sh`）を追加。** プロジェクト内のすべての `.rs` で自分たちが宣言した名前を走査し、
一文字の名前とよくある略語を判定する（略語リストは 144 条、それぞれ「何と書くべきか」付き）。プロジェクトは `.claude/abbreviations` に
ドメイン略語を登録し、自前の番号を類として登録することもできる（`e<数字>`）。走査しないディレクトリや単独のファイルは `.claude/naming-lint-exclude` で宣言する（旧いコードを洗い直すときはファイルごとに並べ、直すたびに一行消す）。
どちらも理由が要る。行単位の免除は `// naming-lint:external <理由>`。トレイト実装のメソッド名、`extern` ブロック、Cargo が決めるファイル名は判定しない。
シェルスクリプトの名前はまだ検査になっておらず、`gate.sh` が未実装の段階に挙げている。
標本は 12 組。複製に対して 39 条のミューテーション（免除を一つ外す、宣言箇所の一種類を外す、コメントや文字列の剥がしを一段外す）をかけ、すべて標本が捕まえた。
singlefs の research コードで試走したところ（119 ファイル、16792 個の名前、略語は未登録）7246 件を報告した。各種の命中を抜き取って見て、
誤検知二種類を直した：Cargo が決める `lib.rs` と、名前の途中にある英語の冠詞 `a` である。

**`scripts/check.sh` の clippy にコーディング規律の lint を七つ追加**：`wildcard_enum_match_arm`、`allow_attributes_without_reason`、
`cast_possible_truncation`、`cast_sign_loss`、`cast_possible_wrap`、`undocumented_unsafe_blocks`、`shadow_unrelated`。
標本クレートで実測：それぞれ違反を一箇所ずつ置くと七つとも赤になり、細則どおりに書いた版は書式・clippy・ビルド・単体テストの四段階すべて緑だった。

**`doc-discipline.md`、`writing-economy.md`、`writing-style.md` を `rules/writing-discipline.md` に統合。**
三編とも人が読むために書く文章（誰のためか、どの長さか、どう言うか）を扱っている。統合版は一行ずつ突き合わせ、三編の本文は一文も失っておらず、
削ったのは三編の間で互いを指し示していた文だけである。三編を参照していた箇所は新しいファイルを指すよう改めた。

**doc-lint の規則一覧の検査をテンプレートとプロジェクトへ広げた。** これまでは SOP リポジトリの `CLAUDE.md` と `rules/` が一項目ずつ一致するかだけを見ていた。
今は `templates/CLAUDE.project.md`、およびプロジェクトの `CLAUDE.md` と導入済みの複製の対応も、同じ判定基準で見る。
これを迫ったのは実測の欠落二つである：テンプレートは 0.0.31 で `pushback-discipline` が加わって以来それを一度も参照していなかった。
singlefs の `CLAUDE.md` は `engineering-philosophy`、`sop-first`、`pushback-discipline`、`writing-style` を一度も `@` しておらず、
この四条は singlefs のセッションで一度もコンテキストに読み込まれていなかった。テンプレートは補った。

`GLOSSARY.md` は「略語」「経路数」の注を改め、「略語登録表」「ワイルドカードアーム」の二項を加えた。

## 0.0.40 — 2026-09-10

**ゲート段階「CHANGELOG 連続性」（`scripts/changelog-lint.sh`）を追加：どの版にも自分の節があり、
最新の節が `VERSION` であること。** バージョン規律が問うのは「規範本体を変えたのに `VERSION` を
上げていないか」だけで、CHANGELOG が追いついているかは問わなかった。実測：0.0.39 では `VERSION`
が 0.0.35 から 0.0.39 に上がったのに、三言語のリポジトリの CHANGELOG がそろって 0.0.36 を飛ばし、
その版の二つの変更はどの節にも記録されていなかった——ゲートは全緑で、diff を段落ごとに突き合わせて
初めて見つかった。

判定はファイル全体に対して行い、diff の窓では見ない：二段見出しは `## x.y.z — YYYY-MM-DD` だけで、
最後の一つは「x.y.z 以前」のような日付なしの締めでもよい。最新の節は `VERSION` と一致すること。
隣り合う二節は直後の版でなければならない（パッチ +1、マイナー +1 でパッチを 0 に、メジャー +1 で
残りを 0 に）——番号飛び・重複・逆順はどれも赤で、節が一つも無いのも赤。SOP リポジトリ自身でだけ走り、
各言語リポジトリはそれぞれ自分の CHANGELOG を判定する。`CLAUDE.md` の冒頭に一文を足し、
`skills/gate/SKILL.md` の「SOP リポジトリ自身でしか走らない段階」にこれを加えた。

## 0.0.39 — 2026-09-10

**`rules/verify-before-claiming.md` に一節を追加：狭いほうを確認し、口に出したのは
広いほうだった。**

この文書はこれまで二つを扱っていた——外部の状態を述べる前にその場で確認すること、
そして「決まっているか」と「実際に何と定義されているか」は別の問いであること。
どちらも失敗の形は**確認していないこと**だと前提している。この節が扱うのは
**確認したのに失敗する**ほう——コマンドは走らせた、ファイルも読んだ、
検証済みの命題を手にしている。**誤りは、そのあとに述べた文がそれより広いことにある。**

判定基準：これから言う文の主語と範囲は、いま確認したものとまったく同じか。
「この経路は通らない」を確認したなら言えるのはそれだけで、「できない」ではない。
「このファイルには書かれていない」を確認したなら言えるのはそれだけで、
「リポジトリのどこにもない」ではない。やり方は、広いほうの文を証明すべき命題として置き、
「これが真であるためにはどの場合を排除する必要があるか」を問い、一つずつ潰す。
潰しきれなければ、実際に潰した範囲まで文を縮める。

singlefs での実測（2026-09-10、同日・同一人物で 2 件）：
(1)「`sudo` がサンドボックスに阻まれた」を確認 ⇒「root が取れないのでこの観測はできない」
と記した。しかし同じリポジトリ内の別経路は**その権限を必要とせず**、
文書にも「実測済み」と一字一句書かれていた ⇒ 観測ゼロの一巡、記録された理由が偽。
(2)「あるクラス規則の列挙にこの対象が載っていない」を確認 ⇒
「この枠はリポジトリ全体でどこにも根拠がない」と記し、それを土台に実験を丸ごと立てた。
しかし**クラス所属は別の 2 ファイルに書かれており**、そのクラス規則は最初から覆っていた
⇒ 立て方が無効となり、同じ未払い項目を二度書き間違えた。

⇒ 節の末尾にもう一点：「この対象は条項に覆われているか」は特に嵌まりやすい。
**条項は別の場所にあってクラス単位で覆っていることがある**——対象自身の節には一字もないまま。
判断する前にその名前でリポジトリ全体を grep し、既存のクラスに入れられていないか見ること。

## 0.0.38 — 2026-09-10

**二件、いずれも既存の節への追加。どちらも「条項はまだあるが、もう効いていない」族。**

**`rules/evidence-discipline.md`「走らせ直したら本文に照らし返す」に第二の形態を追加：
数が漂ったのではなく、限定詞が消えた。** もとの節は数値の漂流だけを扱っていた——
本文が 1.475 倍、成果物が 1.625 倍で、区間アサーションでは捕まらない。
しかし本文と成果物の食い違いにはもう一種ある——**どの数も真で、結論だけが
成果物の支える範囲より広い**。本文の表がパラメータの次元を丸ごと落としているからだ。
数が漂ったなら並べる数が二つ残るが、限定詞が消えると**照合対象そのものがない**。
判定基準：結論が「A でなければ得られない」と言うなら、成果物で
**A でない腕の各パラメータ点の値**を読む。本文の表にその列がなければ「〜でなければ」は成立しない。

singlefs での実測（2026-09-09）：ある実験の本文は「集約でしか得られないのは回収の枠だけ……
他の二腕は一つも空けられない」と書いていたが、同じ保存成果物では別の腕が
非交錯の点で**升目ごとに同一**だった。本文の表に交錯ステップの列がなかった。
**装置のアサーション射程はずっと正しかった**——単体テストがそのパラメータを固定し、
アサーションメッセージにも書いてあった——射程を落としたのは本文のほうである。
再実行はバイト一致、**ゲートは緑**。その結論はすでに下流三箇所に継承されており、
うち一つはそれを根拠に決まった決定項目そのものだった。

**`rules/test-discipline.md`「失敗条項が結論を反証不可能にしてはならない」に裏面を追加：
前件は逆に書けてしまい、逆に書くと二度と発火しない。** 本則が扱うのは
「条項のせいで結論を覆せない」こと。こちらが扱うのは「条項自体が発火しえない」こと。
逆に書かれたものは**誤り**の顔をせず、**発火しなかった**の顔をする——
そして巡回報告の上では「この条項は発火しなかった」と
「この条項は確認済みで結論に問題なし」は見分けがつかない。
やり方：**失敗条項を書いたらすぐ、どんな観測がそれを発火させるかを一文で書く**。
書けなければ、逆に書いたか空に書いたかのどちらかである。

singlefs での実測（2026-09-09）：事前に固定した条項が「ある腕の利得が**非交錯**負荷でのみ
現れる場合 ⇒ 条件は不明として記録する」と書かれていたが、その巡回の発見は
利得が**交錯**負荷でのみ現れることだった ⇒ 前件は恒偽、一度も発火せず、
本来は発火すべきだった。条項を書いた本人は二度自己点検して気づかず、
別の脚が前件を一字一句突き合わせて撃ち抜いた。

## 0.0.37 — 2026-09-09

**`rules/evidence-discipline.md`「先に結論を決めてからモデルを組んではならない」に一節追加：
腕の定義も「走らせる前に固定する」対象であり、失敗条項が当たったときに許される道は一つだけ。**

従来の条文は禁止だけを与えていた——判定基準・閾値・無効条項は走らせる前に固定する、
「後から判定基準を変えてはならない」「後から規則を緩めて勝ちを宣言してはならない」——が、
**許される道が書かれていなかった**。そのため失敗条項が実際に当たると、残る道は二つだけになる：
当たらなかったことにするか、その回を丸ごと破棄するか。さらに腕の書き方が曖昧だと、攻撃は
**最も弱い読み方**を突く。そこで腕を強い読み方へ「明確化」して勝ちを宣言するのは、
判定基準を一文字も変えていなくても実質は事後のモデル作りである。

追加した三手順：最も弱い読み方で**負けを一度記録する**、**厳しくする方向のみ**（元の判定基準で
同等以上に厳しく判定されること）、**どこを厳しくしたか書く**（何を余分に要求するのか言えなければ
それは緩和）。最初の手順が最も省かれやすいこと、省いた瞬間に誠実な厳格化と「緩めて勝ちを宣言する」
が紙の上で完全に同じ形になることも明記した。

singlefs での実測（2026-09-09）：三方論証の一回で、逆推の脚が走らせる前に固定した失敗条項に従い
一つの腕を失格と判定した。理由は「フィールドの並び順」を覆えないこと。確認すると、登録された腕の
文面はスカラーだけを射影するとは述べていなかった——当たったのは最も弱い読み方だった。
判定は三手順に従い、厳しくした形をその決定の本文に書き込んだ。

## 0.0.36 — 2026-09-09

**`rules/evidence-discipline.md`「数や結論を撤回するときも、その場で誰が引いているかを洗う」の
あとに裏面の一節を追加：撤回の理由が崩れても、撤回された結論が自動的に戻るわけではない。**
撤回は一つの裁定であり、理由が崩れたことはその裁定の根拠が消えたことを示すだけで、
反対側が成り立つことは示さない。元の結論を戻すには論証をやり直す必要がある。
さらに撤回のあと、その枠にはたいてい別の理由がすでに立っており、撤回された方だけを見ていると
それを丸ごと見落とす。やり方は順に三つ問う：今この枠を支えている理由はどれか、
それ自体は立つのか、撤回された方を復活させる論証は実際にやったのか。

**`skills/decide/SKILL.md` に第 7 項を追加：未決項の問い方は、読み方が一通りでなければならない。**
各読み方を別々に書き出し、答えが違うなら問い方はまだ決まっていない——先に問いを決め、
それから理由を論じる。

singlefs での実測（2026-09-09）、二件は同じ枠から出ている：ある理由が 2026-09-06 に
「X と両立しない」として撤回され、X は翌日取り消された。問うた側はそれを根拠に判断し直しを求め、
方向は復活を指していた。三方論証の逆推の脚が突いた三点は、どれも両立不能がまだ生きているか
どうかとは関係がない——別の文書が同日、両立不能に依存しない代替の理由を残していた。
その代替の理由の二つの支点はそれぞれ折れていた。そしてこの枠が問うているのは、撤回された理由が
答えていた事柄ではなかった——問い方「同等の完全性を要するか」には二通りの読み方があり
（その物自体のバイトか、それが指す先か）、撤回された理由は前者に答え、文言は後者を問うていた。
その枠は三日間、二巡の論証を経ても動かず、問い方を確定させた途端その場で閉じた。

## 0.0.35 — 2026-09-06

**`rules/show-me-test.md` の「踏んだ落とし穴は失敗する検査にする」節に、射程についての
小節を追加：一つの装置の中に立てた検査は、次の装置を縛らない。** 言明もミューテーション
テストも、それが属する装置の中でしか効かない。別の装置を立てて同じことを一から組み直せば、
その検査は一言も言わない。

singlefs での実測（2026-09-06）：ある計数モデルが「容量 × 充填率」という予算を一方の種類の
対象の個数と取り違え、その上にもう一方を足した——ディスク上に 126% 分の対象を並べたことに
なる。同日中に直し、赤くなる言明も一つ残した。数時間後、別のモデルが同じ誤りを繰り返し
（135%）、その言明は別の装置にあったため一言も言わなかった——新しい装置は単体テスト全緑、
ミューテーションも全部捕捉、ゲートも全緑で、二つのモデルが同一の物理量について 1.5 倍違う数を
報告しながら突き合わせるものは何も無く、誤ったほうの数はすでに決まった条項に書き込まれた。

そこで判定基準を「この落とし穴を赤くなる検査にしたか」から「**この落とし穴はどの層にあるか**」
に改めた：あるコードの挙動の中にある落とし穴は、そのコードの隣の検査で足りる。
**モデル化の計測条件**（同じ量をどう数えるか）にある落とし穴には、二つの装置を同じ数に
落ちさせる**装置をまたぐ検査**が要る。手早い判定基準は「別の装置を立てて一からやり直したとき、
また踏むか」である。あわせて、落とし穴を直した本人がいちばん手を止めやすい地点も明記した：
彼は確かに赤くなる検査にしており、根拠は揃い、ゲートも全緑なので、その検査の射程を
もう一度考えない。

**`rules/evidence-discipline.md` の「産物を引くときは行ごと写す」節に小節を追加：
走らせ直したら本文に照らし返す。リプレイが緑でも本文が正しいことにはならない。**
リプレイが留めるのは結論がどの区間に落ちるかであり、区間が広いだけ本文はその中でずれられる。
実測：ある実験の本文が 1.475× / 22.33× / 5 123 506 ns と書いた一方、同じ実験の保存産物には
1.625× / 30.36× / 5 199 857 ns とそのまま記されており、三つとも区間言明の内側で、
ゲートは全緑だった。同じ小節は「単体テスト N 件」「ミューテーション M 件」「産物 K 行」の
ような、コマンド一つで数えられる主張も対象にする——同じ日に同じリポジトリで、
その種の主張が原典や産物と食い違う箇所が五つ見つかった。

どちらも規則本文のみで、門番の段は増やしていない：装置をまたぐ検査には、まず産物側に
「この数はどの量か」を機械可読で注記する仕組みが要るが、本パッケージには無い。
計数の主張の機械判定は、各プロジェクトのローカル段に置く。

## 0.0.34 — 2026-09-06

**`rules/evidence-discipline.md` の「新しい判定基準を立てたら既存の項目に振り直す」節に、
小節を一つ追加：数や結論を撤回するときも、その場で誰が引いているかを洗う。**
同じ規律で対象だけが違うが、こちらのほうが見つけにくい——撤回した本人はたいてい数箇所は
実際に洗っており、だから洗い終えたと考える理由がある。そして漏れた一箇所は数日後に表に出る。

singlefs での実測（2026-09-06）：位置エントリの幅を書き換えたとき、その決定本文自身が
「本項が定めるのは、これまで決定の手続を一度も通っていないのに、すでに消費されていた数である」
と述べ、下流の派生値二つを名指しで洗った。三日後、別の文書の三つ目の引用は旧値のままで、
ある三方論証の背景資料に書き写され、三本の脚がそろって継承し、その数に乗っていた論証は
その回すべて無効になった。二箇所を洗い、一箇所を漏らし、本人はそれを知らなかった。

判定基準は「リポジトリでその数をまだ使っているものの一覧を出せて初めて撤回し終えたとする」
と書いた。範囲は記憶ではなくリポジトリ全体とした——記憶が差し出すのは見つけやすい数箇所だけ
だからである。あわせて、これが既存の「多方論証に与える背景資料はそれ自体を先に検める」と
同じ穴の両端であり、両端を塞いで初めて塞がることも明記した。

規則本文のみで、門番の段は増やしていない：今日の時点で機械判定の形が無い（ある数が
「撤回された」と判定するには撤回の登記が要るが、本パッケージには無い）。
機械判定が要る利用側は、各自のローカル段に置くこと。

## 0.0.33 — 2026-09-06

**`install.sh` が「プロジェクトがこのファイルを引き取った」と「プロジェクトが上流に
遅れている」を区別できるようになった。** これまでは「上流のテンプレートと異なる」しか
見ておらず、二つは同じに見えていた。しかし kb の骨組みや skill のスタブは、
そもそもプロジェクトが書き換えるためのものである——結果として
**自分の kb に手を入れたプロジェクトは、初回導入のあと二度とバージョン印を更新できなくなり**、
ゲートの段階 0 が赤のままになる。singlefs で実測：12 件の「遅れ」はすべて
プロジェクト自身の編集だった（`.claude/kb/INDEX.md` は 36 行がプロジェクト独自の文面）。

プロジェクトは引き取ったファイルを `$ROOT/.claude/install-owned` に書く。
一行に一件、`<相対パス>  # 理由`。**理由は必須**：ファイルを引き取るということは、
以後そのファイルへの上流の変更が届かなくなるということであり、
理由の欄はそれを正面から引き受けさせるためにある。一覧は必ず出力に報告する——
黙って数件だけ比較を飛ばすのは、この防護が未実装なのと見分けがつかない。

赤になるのは二つ：理由が書かれていない場合と、`install.sh` が置きもしないパスを
書いた場合（その項目は何の効果も持たないのに、そのファイルが免除済みだと
人に思わせてしまう）。**壊れた一覧はその場で止める**。バージョン印はその先へ進まない。

`README.md` の「バージョン印は毎回更新される」という一文は誤りだったので併せて直した——
`install.sh` の実際の動作とはとうに食い違っていた。

変異は五件を個別に実行した。うち一件が盲点を露わにした：「壊れた一覧は止める」の一段を
丸ごと削っても、既存のサンプル二件は**揃って緑のまま**だった——それらは同時に内容も
遅れており、赤くしていたのはそちらの経路だったのである。他に遅れの無い場面を
用意して初めて切り分けられた。自己検査は 182 → 188 件。

## 0.0.32 — 2026-09-06

**gate-lint に三つ目の拒否形態を追加した：直に印字される `✗`。** これまでは `lib.sh` の
`bad` と `die` しか見ていなかったが、**プロジェクト固有の段階の多くは `lib.sh` を
source しない**——直に `echo "  ✗ …"` と書くか、判定を埋め込み python の
`print('  ✗ …')` に書く。この一群は一度も検査されていなかった。

singlefs のローカル段階で実測：この形の拒否が 46 箇所あり全て免除されていた。
検査を入れると **14 箇所は次の一手が一言も無い**（`15-research-build.sh` の「cargo が無い」、
`70-citations.sh` の「$S が見つからない」——どちらも何をすべきか言わずに `exit 1`）。
gate-lint が見る拒否の数は 125 → 170 になった。

判定の二箇所は実データに合わせて較正した：

- **窓は 5 行ではなく「次の拒否まで」。** python は先に `✗` を印字し、該当項目を繰り返しで
  並べ、最後に対処を書くことが多い。5 行ではこの一群をまとめて誤判定する
  （サンプル `printrejok` が見張る）。
- **heredoc 本体は既定ではコードとして読まないが、解釈器 heredoc は読む。**
  `python3 - <<'PY'` の中の `print` は実際に提出者へ表示される。従来どおり読み飛ばせば、
  python で書かれた段階は依然として一括で見えないままになる（サンプル `printrejpy` が見張る）。

二つの免除はどちらも明示的に書くこと。推測はしない：繰り返しの中の一件ずつの行には
`# gate-lint:detail`、要約行には `# gate-lint:summary`。

`rules/sop-first.md` の拒否形態表に三行目を追加した。サンプル四件、変異四件を個別に実行し、
それぞれちょうど一つの用例に捕まった。自己検査は 178 → 182 件。

⚠️ **この検査により singlefs のゲートは 14 箇所で赤になる。** すべて同プロジェクトの
`.claude/gate.d/` の中である。上流の三リポジトリには影響しない——共有パッケージには
直に印字される拒否が一つも無く、すべて `bad` を通っている。

## 0.0.31 — 2026-09-06

**新しい規則 `rules/pushback-discipline.md`：提案は誰が出したかによって検査を免れない。**
利用者が出した案も、他所から来た案と同じ関門を通る。実測データ、あるいは既に確認済みの
事実と食い違うときは、手を付ける**前に**三つを揃えて言う：食い違う相手はどれか（出典付き）、
提案どおりにすると何が起きるか（どんな観測でそれが分かるか）、第三の道があるか。
相手が明確に言い直したらそのまま実施し、もう説得しない。ただし**警告は必ず記録に残す**。
**やり終えてから食い違いに気づいた場合も同じく言い、必要なら丸ごと撤回する**——
費用は利用者が負い、コードは正しさに責任を負う。「ここまで作ったのだから」は続ける理由にならない。
コードの変更に取り消せないものは無い：撤回の代償はかつて人の時間だったが、今その部分は
機械の仕事であり、数万行と数百行に桁の違いは無い。本当に戻せない `git checkout`・
`rm -rf`・外部への恒久的な約束は `command-safety.md` が扱う。

**警告は `.claude/warnings/<日付>.md` に書く。** 日付ごとに一ファイル、警告ひとつにつき
`##` 節ひとつ、四項目すべて：提案 / 異議 / 既知のリスク / 結末。他の場所からは必ずここへ
張り、二部目は作らない（`kb-discipline.md` 第 4 条）。日付ごとに分けるのは
**古いファイルを直さずに済ませるため**である——警告は書いた時点でその日の記録になる。
`install.sh` がプロジェクト側にこのディレクトリを用意する。

**doc-lint に二つの検査を追加した**：

- `.claude/warnings/` 下のファイル名は `YYYY-MM-DD.md` であること、各 `##` 節に四項目が
  揃っていること。項目名は言語ごとに取り、三言語とも走る：固定の項目名を見るのであって
  字形の黒名簿で語義を当てているのではないため、「語彙表が無いので未実装」の段には入らない。
- `rules/*.md` と `CLAUDE.md` 中の `@rules/` 参照は一項ずつ一致していること。
  どちらの向きも無言で失敗する：参照されていない規則は文脈に入らないのに、
  効いている規則と見た目が変わらない。存在しないファイルへの参照は、
  その行が展開されないだけで、CLAUDE.md は完全に読めてしまう。
  この検査は今回自分が必要としたものである——規則を一つ足して CLAUDE.md に
  書き忘れても、これまでは何処も赤くならなかった。

サンプル七件、変異五件を個別に実行し、それぞれちょうど一つの用例に捕まった。
自己検査は 171 → 178 件。

## 0.0.30 — 2026-09-06

**en / ja 両リポジトリのゲートが赤かったのを直した。** 二箇所とも、
一組の規則を多言語リポジトリに複製したときに空いた穴である：

- `doc-lint` のサンプルは中国語で書かれているが、`scripts/` は各言語リポジトリへ
  バイト単位でそのまま複製される。その結果 en リポジトリでは、同じサンプルが
  `## Revision history` と突き合わされ、44 件が一斉に偽陽性になった——
  doc-lint 自体は一文字も壊れていない。言語は**サンプルの属性**であって
  リポジトリの属性ではない。`selftest` は `DOC_LINT_LANG=zh` でサンプルの言語を固定する。
  この口は判定基準を差し替えられるので、**設定したら必ず出力に出す**。
  その一行が残っているかを見張る自己検査も一件追加した。
  0.0.25 で判定基準を言語別にしたときに空いた穴で、en / ja はそれ以来ずっと赤だった。
- en リポジトリは `templates/kb/` の D1 と E1 の略称の雛形をどちらも
  `<name, 24 characters or fewer>` と訳しており、「一つの略称を二つの採番で共有してはならない」
  に抵触していた。それぞれに名詞を付けた。zh / ja は元から別々の訳になっている。

**doc-lint に除外一覧を追加した：そのまま保存した証拠は後から書き換えてはならない。**
`research/prompts/` のようなディレクトリには、モデルに渡したプロンプトがそのまま入っている。
産物と一対一で対応しており、一文字直せば産物はもうその入力に対応しない。
プロジェクトは `.claude/doc-lint-exclude` で宣言する。一行に一件、**それぞれ理由を書くこと**。
存在しないディレクトリを指す項目、一件も除外できていない項目は赤とする——
効いていない除外項目は、そのファイル群がもう避けられていると人に思わせてしまう。
除外は必ず出力に報告する：黙って二百件を読み飛ばすのは、この検査が未実装なのと見分けがつかない。
規則は `rules/evidence-discipline.md`。サンプルは七件で、除外宣言を持たない対照が一件含まれる。

## 0.0.29 — 2026-09-06

**「已被 X 覆盖」の判定が、この SOP が管理する採番だけに反応するようになった**
（`D` 決定 / `E` 実験 / `C` 未済チェック / `I` 不変条件 / `A` 前提 / `O` オラクル）。
実測した誤検知：ディスク上のバイトについて述べた一文「U1 を再構築するには U2 の元の
バイトが要る——それは既に U9 に上書きされている」が、「本文で『X に上書きされた』と
その場に注記してはならない」に当たると判定された。この文の「覆盖」はバイトが書き潰される
ことであって、条項が覆されることではない。二つの語義が同じ語に重なっており、
両者を分けられるのは「上書きした側が採番された条項かどうか」だけである。

## 0.0.28 — 2026-09-05

**件数の判定が python の f-string も認めるようになった。** shell の `$n` しか
認めないと、python で書かれた段階が軒並み偽陽性になる——成功の一文が python 側で
整形されるためである（singlefs の二つの段階で実測）。サンプルは `countpy`。
`nocount` は依然として赤で、判定基準は骨抜きになっていない。

## 0.0.27 — 2026-09-05

**0.0.26 が自ら持ち込んだ無限ループの修正と、selftest への時間切れ保護。**

- `gate-lint` が算術展開を取り除く際、対になる `))` は `$((` の**後ろ**だけで探す。
  行全体から最初の `))` を探すと止まらなくなる：`if ((okc)); then …; pass=$((pass+1)); fi`
  のような行では最初の `))` が `$((` より前にあり、切った後の文字列も `$((` を含んだまま
  短くならない。実際の資料で即座に踏んだ——9 分走っても終わらず、赤でも緑でもなかった。
  サンプル `arith2` は最後まで走り切り、かつ緑でなければならない。
- `selftest` のサンプル実行箇所二つに `timeout` を入れた（既定 60 秒、`SELFTEST_TIMEOUT` で変更可）。
  **固まった検査は赤い検査より危険である**：ゲートの出力の中では何物でもなく、ただ戻ってこない。

## 0.0.26 — 2026-09-05

**プロジェクト固有の段階が初めてゲート自身の管理下に入り、singlefs の三日分の作業から
剥がした三つの規律を追加した。**

**一、`.claude/gate.d/` を二つの lint の走査対象に入れた**

`gate.sh` がプロジェクト固有の段階ディレクトリを `gate-lint` と `shell-lint` に渡す。
従来はどちらの走査範囲にも入っていなかった——それらのスクリプトも共有段階と同じように
提出者を拒否するのに、一度も検査されていなかった：最初に通した時点で出口の無い拒否が
7 件出た（singlefs 実測）。

**二、gate-lint に三つ目の検査：成功の一文に何件検査したかを出す**

「0 件走査」は通過ではない。判定基準の走査範囲を少し狭く書くと、対象が全部その最初の
一歩で飛ばされ、通過でも失敗でもなく末尾は緑を報告し続ける（singlefs の C114：ある段階の
第 3 項がまさにその状態で緑だった）。**成功を報告している**スクリプトだけを判定し、
本当に対象を走査しないものは `# gate-lint:nocount <理由>` と書く。
サンプルは `nocount` / `countok`。本パッケージ自身の `show-me-test.sh` が最初に咬まれた。

**三、gate-lint の偽陽性を一つ修正**：`bad=$((bad + 1))` の `bad ` は `(` の直後にあり、
CMD_POS では一つの拒否と読めてしまう。判定前に算術展開を取り除く。サンプルは `arith`。

**四、規則の追補**（いずれも実測の代価つき）

- `evidence-discipline`：**産物を引くときは行ごと写す**（同じ差分が四ラウンド連続で誤伝）、
  **新しい判定基準はその場で既存項目に振り直す**（物差しを新参にだけ振っていた）、
  標本の表に一行（「同じ産物の中に反例が無いか」）、藁人形の節に
  「ある腕の数値を引くときは定義も一緒に」。
- `show-me-test`：赤くなることを示す条は**設計上の論証にも効く**——
  必ず非ゼロを報告する世界を先に作ってからルールを直す。
- `skills/decide` 硬要件 6：**人が裁定した一文を動かしたら `checks-owed.md` に
  未返済の一件を記す**。

## 0.0.25 — 2026-09-02

**訳文リポジトリに残っていた最後の中国語を一掃し、`agents/` の層を立てた。**
どちらも「注意書き」ではなく、赤くなる検査として入れた。

**一、skills / templates を「原様複製」から「一篇ずつ翻訳」へ**

これまでは `i18n-sync` の `SHARED` にあってバイト単位で複製されていた。その結果、
本リポジトリの skill 本文・プロジェクト骨格・litmus のコメントはすべて中国語であり、
しかも `templates/CLAUDE.project.md` は `install.sh` が利用者のプロジェクト直下の
`CLAUDE.md` として書き出すものだった。「日本語で作業する人が中国語を読む必要はない」と
真っ向から矛盾する。

- マニフェストは 14 条から 28 条へ：`CLAUDE.md`、`rules/`、`agents/`、`skills/`、
  `templates/`（`*.litmus` を含む）。判定基準は一つ、「人が読む散文が含まれているか」。
- **網羅性の検査を新設**：どちらの一覧にも入らない文書はその場で赤になる。
  これは「空白」の機械判定可能な形である——`.md` を足して翻訳の要否を決め忘れても、
  ゲートが思い出させる。
- **来歴マーカーの位置と注釈形式をファイル種別で決める**：`SKILL.md` の YAML
  frontmatter は第 1 行から始まらなければならない（その上に置くと skill は黙って
  入らなくなる）。`.litmus` の第 1 行は `C <名前>` でなければならず、さもないと
  herd7 が `splitter error in sublexer first line` を出す（herd7 を実際に走らせて確認）。
  押印後に読み戻して確認し、両方の落とし穴を赤くなる検査にした。
- **`install.sh` はプロジェクトへ配置する際に来歴マーカーを剥がす**：それは配布層の
  帳簿であり、利用者のプロジェクトへ写せば決して更新されない古い注記になる。しかも
  相手がこれから編集するファイルに貼り付く（実際にインストールして判明）。
- 訳文 28 篇（en / ja 各 14 篇）を書き直した。三言語の litmus を herd7 で実走させ、
  判定は一致。

**二、`agents/` を統治下に入れる**

`agents/INDEX.md` に、何を置くか（`skills/` との境界：読むものか、委譲するものか）、
書き方、三つの規律、つなぎ方を記した。マニフェストに入れて一篇ずつ翻訳し、`GOVERNED`
に入れ、`install.sh` は agent スタブを配置する。空は状態なので口に出す：ディレクトリが
空のあいだ `manifest.sh` は「統治下にあり現在は空」と報告し、黙って通過させない。
doc-lint も、agent 定義に履歴節を残さないこと、`name`（ファイル名と一致）と
`description` を持つことを要求するようになった——どちらも欠けてもエラーにならず、
ただ静かに効かなくなるからである。

**三、ついでに直したもの** — いずれも自己検査に標本を食わせて露出した

- `manifest.sh` の `gen()` がパイプラインの中で `cd` を落としていた。リポジトリ直下から
  走らせると cwd がたまたまパッケージ直下と一致するので、表に出なかった。
- 存在しないディレクトリに対する `find` が `translated_paths` 全体を失敗させていた。
  `2>/dev/null` はメッセージを隠すだけで終了コードは隠さない。`--update` は終了コード
  1 で、しかも一文字も出力しなかった。
- `stamp_read` は sed を使っていたが、BRE の `\|` が区切り文字と衝突する。黙って何にも
  一致せず、押印後の読み戻し断言でようやく捕まった。awk に変えた。

判別力：用例 111 → 120。新しい検査はすべてミューテーションで検証した——変更前は緑、
変更後は赤。うち五つは今回の作業自身が作った盲点（網羅性の検査、第 1 行が押し下げられる、
install での剥がし、agent の `name`、`GOVERNED` の `agents/`）であり、標本を用意して初めて
噛むようになった。

作業上の記録を一つ：今回「マニフェストだけ写して訳していない」が二度起きたが、どちらも
自分の手によるものである。一括編集スクリプトが途中で止まり、片方の言語の編集は走らず、
それでも両方に押印され、ゲートは全緑だった。来歴マーカーが示せるのは「誰かが訳したと
主張している」ことだけで、「実際に訳した」ことではない。内容を一字ずつ突き合わせて
初めて分かった。

未着手：GLOSSARY の「説明」列は依然として参照リポジトリの言語である——本パッケージに
残る最後の言語の欠落であり、当該ファイル冒頭に明示して記録してある。

## 0.0.24 — 2026-09-02

**この版は単独では公開していない。0.0.25 と同じコミットで出る。**
下流から見るとバージョンは 0.0.23 から 0.0.25 へ飛ぶ。0.0.24 を探さないよう、ここに記す。

ミューテーションテストによる第二回の敵対的監査とその一括修正。**主題は判別力**：
前回追加したゲート検査のうち六条は、まるごと削除しても `selftest.sh` が
54/54 で緑のままだった——検査はあるが、検査を見張るものがなかった。

- **判別力**：doc-lint の履歴記述パターン群 / `CLAUDE.md` の履歴節 / kb の履歴節、
  gate-lint の `howto` 窓幅、lkmm の静的検査二条、show-me-test の注釈の広さに、
  それぞれ単一目的の標本を用意。`blind` の `want` は六条の検査が共有する
  「本文では禁止」だったため、検査ごとに分割した。八つの変異はすべて赤になる。
  用例 54 → 69。
- **doc-lint の死んだパターン**：`'[〔【\[]已废弃[〕】\]]'` は GNU grep では決して
  一致しない——POSIX では角括弧集合の中の逆斜線は字義通りだからである。修正した。
- **`die` はもう免除されない**：`die` は `bad` + `exit`、すなわち拒否である。
  `lib.sh` の `die` を「メッセージ + 対処」の二段にし、gate-lint は一文しか持たない
  `die` を赤とする。17 箇所の呼び出しに対処を補った。
- **`shell-lint.sh` を新設**：command-safety のうち機械判定できる二条を赤くなる検査に——
  パターン一致でプロセスを殺すこと、サブシェル内の代入で値を外に持ち出すこと。
- **`qemu/run.sh` の `$QEMU_LOG`**：常に `$( )` の中で走る関数の内側でしか代入されず、
  親側の六箇所の参照は `set -u` の下で `unbound variable` だった——
  **五つの失敗分岐が `howto` を出す前に落ちていた**。作業ディレクトリは呼び出し側が持つ。
  これは command-safety 自身が記している落とし穴そのものである。
- **gate スキルの「赤くなることを示す」例が実測で緑だった**：`>>` は「改訂履歴」の
  見出しより後ろに追記され、本文走査はそこで止まる。差し替えた。
- **規範本体の定義は一箇所**：`version-discipline.sh` の `GOVERNED` を正とし、
  `CLAUDE.md`・README・session-wrapup がそれぞれ別の一覧を持っていたのをリンクに変えた。
  `README.md` と `I18N` を管理下に入れた。
- 重複と腐り：`CLAUDE.md` と README で重複し既にずれていた多言語の段落を README 一箇所に。
  インストーラは三つではなく六つのものを置く（プロジェクト直下の `litmus/` を含む）。
  gate スキルの段階表を 4 行から 9 行に。「三言語」と書き固めた 15 箇所を
  `I18N` の `languages=` に従う言い方へ。
- `gate.sh`：「上流より古いかは未確認」を集約の「今回走らせていない段階」に出す——
  従来は冒頭で一度警告するだけで、集約は「N 段階すべて通過」と報告していた。
- 規則の追補：sop-first の対処要求が `die` を含むようになった。
  design-doc-discipline は検査コードの落とし穴コメントを例外とする。
  kb-discipline は `INDEX.md` に履歴節を置かないと明記。
  command-safety はどの二条がすでに検査かを示す。
- README を三節から一節に統合。gate スキルの「よくある偽の失敗」表から、
  もう存在しないメッセージ文を差し替えた。doc-lint の死んだパターンは
  `'[[〔【]已废弃[]〕】]'` に修正した。
- 復核後の追加：英語版 `show-me-test.md` で否定が落ちていた
  （「ゲートは誰も正しさに到達しないことを保証する」になっていた）。
  gate-lint と shell-lint がパッケージ直下も走査するようになり、
  `install.sh` の対処のない `die` 四箇所を検出して修正。
  `howto` の窓は `bad` 自身を含めて 5 行であって 4 行ではない。

## 0.0.23 — 2026-09-02

敵対的監査を受けたゲート修正の一括投入（監査レポートは当該セッションの記録）：

- doc-lint：閉じていないコードフェンスを赤に；「履歴節」の後の `##` 節を赤に；
  履歴節内の表による不変条件定義は無効に；rules ファイルの履歴節を赤に；
  rule-definition マーカーは CLAUDE.md / rules / skills に限定；登録済み番号の
  not-numbers 免除を赤に；「脚本文件」「同上游」等の複合語を誤検知しないよう修正。
- gate-lint：`bad` を全形態で認識（単/二重引用符、変数、`;{|&` / `then` / `else`
  の後）；コメント内の howto は対処と見なさない；サマリ免除を
  「失敗/未通過/未過：$カウンタ」に限定。
- Show me test を show-me-test.sh に分離：コメント内の `#[test]` は無効；
  tests/ は `.rs` のみ有効；build.rs もコード扱い；デフォルトブランチでは
  diff 基準を HEAD~1 に後退させ、先にコミットしても「判定対象なし」にならない。
- lkmm：各 Never litmus に同名 `-nofence` の対照ペアを必須化（全体で
  Sometimes が 1 本あれば良い、を廃止）；静的検査を herd7 探索より前に；
  全拒否に howto を追加。
- i18n-sync / manifest：失敗分岐の diff 診断パイプラインが set -e + pipefail で
  スクリプトを途中終了させていた（howto と残り言語の検査が消える）——
  `|| true` で保護；i18n-sync は先にマニフェストの鮮度を検証；GLOSSARY.md を
  共有（そのまま複製）セットに追加。
- version-discipline.sh を新設：規範本体を変えて VERSION を上げなければ赤
  （従来は注意書きのみ）。
- selftest を全ゲートスクリプトに一般化：doc-lint / gate-lint / lkmm の
  サンプルに加え、show-me-test / version-discipline / manifest / i18n-sync の
  スクリプト化ケース、計 54 件。

## 0.0.22 — 2026-09-02

- session-wrapup に「同じリポジトリで他のセッションが飛んでいないか」節を追加；
  show-me-test にミューテーションリスト；test-discipline に「決定的モデル × N 回」
  「ミューテーションテストが証明するのはアサーションが赤になれること」；
  GLOSSARY にミューテーションテスト関連の用語を追加。

## 0.0.21 以前

`git log --oneline` を参照——各コミットメッセージは「バージョン：何を変えたか」
の形式で書かれている。
