# CHANGELOG

規則とゲートのバージョン履歴。`CLAUDE.md` と `rules/*.md` は履歴節を持たない
（design-doc-discipline）ので、履歴はここに記す。個々の変更の詳細は `git log`
を参照——コミットメッセージがそのまま変更説明になっている。

## 0.0.40 — 2026-09-10

**ゲート段階「CHANGELOG 連続性」（`scripts/changelog-lint.sh`）を追加：どの版にも自分の節があり、
最新の節が `VERSION` であること。** バージョン規律が問うのは「規範本体を変えたのに `VERSION` を
上げていないか」だけで、CHANGELOG が追いついているかは問わなかった。実測：0.0.39 では `VERSION`
が 0.0.35 から 0.0.39 に上がったのに、三言語のリポジトリの CHANGELOG がそろって 0.0.36 を飛ばし、
その版の二つの変更はどの節にも記録されていなかった——ゲートは全緑で、diff を段落ごとに突き合わせて
初めて見つかった。

判定はファイル全体に対して行い、diff の窓では見ない：二段見出しは `## x.y.z — YYYY-MM-DD` だけで、
最後の一つは「x.y.z 以前」のような日付なしの締めでもよい。最新の節は `VERSION` と一致すること。
隣り合う二節は直後の版でなければならない（パッチ +1、マイナー +1 でパッチを 0 に、メジャー +1 で
残りを 0 に）——番号飛び・重複・逆順はどれも赤で、節が一つも無いのも赤。SOP リポジトリ自身でだけ走り、
各言語リポジトリはそれぞれ自分の CHANGELOG を判定する。`CLAUDE.md` の冒頭に一文を足し、
`skills/gate/SKILL.md` の「SOP リポジトリ自身でしか走らない段階」にこれを加えた。

## 0.0.39 — 2026-09-10

**`rules/verify-before-claiming.md` に一節を追加：狭いほうを確認し、口に出したのは
広いほうだった。**

この文書はこれまで二つを扱っていた——外部の状態を述べる前にその場で確認すること、
そして「決まっているか」と「実際に何と定義されているか」は別の問いであること。
どちらも失敗の形は**確認していないこと**だと前提している。この節が扱うのは
**確認したのに失敗する**ほう——コマンドは走らせた、ファイルも読んだ、
検証済みの命題を手にしている。**誤りは、そのあとに述べた文がそれより広いことにある。**

判定基準：これから言う文の主語と範囲は、いま確認したものとまったく同じか。
「この経路は通らない」を確認したなら言えるのはそれだけで、「できない」ではない。
「このファイルには書かれていない」を確認したなら言えるのはそれだけで、
「リポジトリのどこにもない」ではない。やり方は、広いほうの文を証明すべき命題として置き、
「これが真であるためにはどの場合を排除する必要があるか」を問い、一つずつ潰す。
潰しきれなければ、実際に潰した範囲まで文を縮める。

singlefs での実測（2026-09-10、同日・同一人物で 2 件）：
(1)「`sudo` がサンドボックスに阻まれた」を確認 ⇒「root が取れないのでこの観測はできない」
と記した。しかし同じリポジトリ内の別経路は**その権限を必要とせず**、
文書にも「実測済み」と一字一句書かれていた ⇒ 観測ゼロの一巡、記録された理由が偽。
(2)「あるクラス規則の列挙にこの対象が載っていない」を確認 ⇒
「この枠はリポジトリ全体でどこにも根拠がない」と記し、それを土台に実験を丸ごと立てた。
しかし**クラス所属は別の 2 ファイルに書かれており**、そのクラス規則は最初から覆っていた
⇒ 立て方が無効となり、同じ未払い項目を二度書き間違えた。

⇒ 節の末尾にもう一点：「この対象は条項に覆われているか」は特に嵌まりやすい。
**条項は別の場所にあってクラス単位で覆っていることがある**——対象自身の節には一字もないまま。
判断する前にその名前でリポジトリ全体を grep し、既存のクラスに入れられていないか見ること。

## 0.0.38 — 2026-09-10

**二件、いずれも既存の節への追加。どちらも「条項はまだあるが、もう効いていない」族。**

**`rules/evidence-discipline.md`「走らせ直したら本文に照らし返す」に第二の形態を追加：
数が漂ったのではなく、限定詞が消えた。** もとの節は数値の漂流だけを扱っていた——
本文が 1.475 倍、成果物が 1.625 倍で、区間アサーションでは捕まらない。
しかし本文と成果物の食い違いにはもう一種ある——**どの数も真で、結論だけが
成果物の支える範囲より広い**。本文の表がパラメータの次元を丸ごと落としているからだ。
数が漂ったなら並べる数が二つ残るが、限定詞が消えると**照合対象そのものがない**。
判定基準：結論が「A でなければ得られない」と言うなら、成果物で
**A でない腕の各パラメータ点の値**を読む。本文の表にその列がなければ「〜でなければ」は成立しない。

singlefs での実測（2026-09-09）：ある実験の本文は「集約でしか得られないのは回収の枠だけ……
他の二腕は一つも空けられない」と書いていたが、同じ保存成果物では別の腕が
非交錯の点で**升目ごとに同一**だった。本文の表に交錯ステップの列がなかった。
**装置のアサーション射程はずっと正しかった**——単体テストがそのパラメータを固定し、
アサーションメッセージにも書いてあった——射程を落としたのは本文のほうである。
再実行はバイト一致、**ゲートは緑**。その結論はすでに下流三箇所に継承されており、
うち一つはそれを根拠に決まった決定項目そのものだった。

**`rules/test-discipline.md`「失敗条項が結論を反証不可能にしてはならない」に裏面を追加：
前件は逆に書けてしまい、逆に書くと二度と発火しない。** 本則が扱うのは
「条項のせいで結論を覆せない」こと。こちらが扱うのは「条項自体が発火しえない」こと。
逆に書かれたものは**誤り**の顔をせず、**発火しなかった**の顔をする——
そして巡回報告の上では「この条項は発火しなかった」と
「この条項は確認済みで結論に問題なし」は見分けがつかない。
やり方：**失敗条項を書いたらすぐ、どんな観測がそれを発火させるかを一文で書く**。
書けなければ、逆に書いたか空に書いたかのどちらかである。

singlefs での実測（2026-09-09）：事前に固定した条項が「ある腕の利得が**非交錯**負荷でのみ
現れる場合 ⇒ 条件は不明として記録する」と書かれていたが、その巡回の発見は
利得が**交錯**負荷でのみ現れることだった ⇒ 前件は恒偽、一度も発火せず、
本来は発火すべきだった。条項を書いた本人は二度自己点検して気づかず、
別の脚が前件を一字一句突き合わせて撃ち抜いた。

## 0.0.37 — 2026-09-09

**`rules/evidence-discipline.md`「先に結論を決めてからモデルを組んではならない」に一節追加：
腕の定義も「走らせる前に固定する」対象であり、失敗条項が当たったときに許される道は一つだけ。**

従来の条文は禁止だけを与えていた——判定基準・閾値・無効条項は走らせる前に固定する、
「後から判定基準を変えてはならない」「後から規則を緩めて勝ちを宣言してはならない」——が、
**許される道が書かれていなかった**。そのため失敗条項が実際に当たると、残る道は二つだけになる：
当たらなかったことにするか、その回を丸ごと破棄するか。さらに腕の書き方が曖昧だと、攻撃は
**最も弱い読み方**を突く。そこで腕を強い読み方へ「明確化」して勝ちを宣言するのは、
判定基準を一文字も変えていなくても実質は事後のモデル作りである。

追加した三手順：最も弱い読み方で**負けを一度記録する**、**厳しくする方向のみ**（元の判定基準で
同等以上に厳しく判定されること）、**どこを厳しくしたか書く**（何を余分に要求するのか言えなければ
それは緩和）。最初の手順が最も省かれやすいこと、省いた瞬間に誠実な厳格化と「緩めて勝ちを宣言する」
が紙の上で完全に同じ形になることも明記した。

singlefs での実測（2026-09-09）：三方論証の一回で、逆推の脚が走らせる前に固定した失敗条項に従い
一つの腕を失格と判定した。理由は「フィールドの並び順」を覆えないこと。確認すると、登録された腕の
文面はスカラーだけを射影するとは述べていなかった——当たったのは最も弱い読み方だった。
判定は三手順に従い、厳しくした形をその決定の本文に書き込んだ。

## 0.0.36 — 2026-09-09

**`rules/evidence-discipline.md`「数や結論を撤回するときも、その場で誰が引いているかを洗う」の
あとに裏面の一節を追加：撤回の理由が崩れても、撤回された結論が自動的に戻るわけではない。**
撤回は一つの裁定であり、理由が崩れたことはその裁定の根拠が消えたことを示すだけで、
反対側が成り立つことは示さない。元の結論を戻すには論証をやり直す必要がある。
さらに撤回のあと、その枠にはたいてい別の理由がすでに立っており、撤回された方だけを見ていると
それを丸ごと見落とす。やり方は順に三つ問う：今この枠を支えている理由はどれか、
それ自体は立つのか、撤回された方を復活させる論証は実際にやったのか。

**`skills/decide/SKILL.md` に第 7 項を追加：未決項の問い方は、読み方が一通りでなければならない。**
各読み方を別々に書き出し、答えが違うなら問い方はまだ決まっていない——先に問いを決め、
それから理由を論じる。

singlefs での実測（2026-09-09）、二件は同じ枠から出ている：ある理由が 2026-09-06 に
「X と両立しない」として撤回され、X は翌日取り消された。問うた側はそれを根拠に判断し直しを求め、
方向は復活を指していた。三方論証の逆推の脚が突いた三点は、どれも両立不能がまだ生きているか
どうかとは関係がない——別の文書が同日、両立不能に依存しない代替の理由を残していた。
その代替の理由の二つの支点はそれぞれ折れていた。そしてこの枠が問うているのは、撤回された理由が
答えていた事柄ではなかった——問い方「同等の完全性を要するか」には二通りの読み方があり
（その物自体のバイトか、それが指す先か）、撤回された理由は前者に答え、文言は後者を問うていた。
その枠は三日間、二巡の論証を経ても動かず、問い方を確定させた途端その場で閉じた。

## 0.0.35 — 2026-09-06

**`rules/show-me-test.md` の「踏んだ落とし穴は失敗する検査にする」節に、射程についての
小節を追加：一つの装置の中に立てた検査は、次の装置を縛らない。** 言明もミューテーション
テストも、それが属する装置の中でしか効かない。別の装置を立てて同じことを一から組み直せば、
その検査は一言も言わない。

singlefs での実測（2026-09-06）：ある計数モデルが「容量 × 充填率」という予算を一方の種類の
対象の個数と取り違え、その上にもう一方を足した——ディスク上に 126% 分の対象を並べたことに
なる。同日中に直し、赤くなる言明も一つ残した。数時間後、別のモデルが同じ誤りを繰り返し
（135%）、その言明は別の装置にあったため一言も言わなかった——新しい装置は単体テスト全緑、
ミューテーションも全部捕捉、ゲートも全緑で、二つのモデルが同一の物理量について 1.5 倍違う数を
報告しながら突き合わせるものは何も無く、誤ったほうの数はすでに決まった条項に書き込まれた。

そこで判定基準を「この落とし穴を赤くなる検査にしたか」から「**この落とし穴はどの層にあるか**」
に改めた：あるコードの挙動の中にある落とし穴は、そのコードの隣の検査で足りる。
**モデル化の計測条件**（同じ量をどう数えるか）にある落とし穴には、二つの装置を同じ数に
落ちさせる**装置をまたぐ検査**が要る。手早い判定基準は「別の装置を立てて一からやり直したとき、
また踏むか」である。あわせて、落とし穴を直した本人がいちばん手を止めやすい地点も明記した：
彼は確かに赤くなる検査にしており、根拠は揃い、ゲートも全緑なので、その検査の射程を
もう一度考えない。

**`rules/evidence-discipline.md` の「産物を引くときは行ごと写す」節に小節を追加：
走らせ直したら本文に照らし返す。リプレイが緑でも本文が正しいことにはならない。**
リプレイが留めるのは結論がどの区間に落ちるかであり、区間が広いだけ本文はその中でずれられる。
実測：ある実験の本文が 1.475× / 22.33× / 5 123 506 ns と書いた一方、同じ実験の保存産物には
1.625× / 30.36× / 5 199 857 ns とそのまま記されており、三つとも区間言明の内側で、
ゲートは全緑だった。同じ小節は「単体テスト N 件」「ミューテーション M 件」「産物 K 行」の
ような、コマンド一つで数えられる主張も対象にする——同じ日に同じリポジトリで、
その種の主張が原典や産物と食い違う箇所が五つ見つかった。

どちらも規則本文のみで、門番の段は増やしていない：装置をまたぐ検査には、まず産物側に
「この数はどの量か」を機械可読で注記する仕組みが要るが、本パッケージには無い。
計数の主張の機械判定は、各プロジェクトのローカル段に置く。

## 0.0.34 — 2026-09-06

**`rules/evidence-discipline.md` の「新しい判定基準を立てたら既存の項目に振り直す」節に、
小節を一つ追加：数や結論を撤回するときも、その場で誰が引いているかを洗う。**
同じ規律で対象だけが違うが、こちらのほうが見つけにくい——撤回した本人はたいてい数箇所は
実際に洗っており、だから洗い終えたと考える理由がある。そして漏れた一箇所は数日後に表に出る。

singlefs での実測（2026-09-06）：位置エントリの幅を書き換えたとき、その決定本文自身が
「本項が定めるのは、これまで決定の手続を一度も通っていないのに、すでに消費されていた数である」
と述べ、下流の派生値二つを名指しで洗った。三日後、別の文書の三つ目の引用は旧値のままで、
ある三方論証の背景資料に書き写され、三本の脚がそろって継承し、その数に乗っていた論証は
その回すべて無効になった。二箇所を洗い、一箇所を漏らし、本人はそれを知らなかった。

判定基準は「リポジトリでその数をまだ使っているものの一覧を出せて初めて撤回し終えたとする」
と書いた。範囲は記憶ではなくリポジトリ全体とした——記憶が差し出すのは見つけやすい数箇所だけ
だからである。あわせて、これが既存の「多方論証に与える背景資料はそれ自体を先に検める」と
同じ穴の両端であり、両端を塞いで初めて塞がることも明記した。

規則本文のみで、門番の段は増やしていない：今日の時点で機械判定の形が無い（ある数が
「撤回された」と判定するには撤回の登記が要るが、本パッケージには無い）。
機械判定が要る利用側は、各自のローカル段に置くこと。

## 0.0.33 — 2026-09-06

**`install.sh` が「プロジェクトがこのファイルを引き取った」と「プロジェクトが上流に
遅れている」を区別できるようになった。** これまでは「上流のテンプレートと異なる」しか
見ておらず、二つは同じに見えていた。しかし kb の骨組みや skill のスタブは、
そもそもプロジェクトが書き換えるためのものである——結果として
**自分の kb に手を入れたプロジェクトは、初回導入のあと二度とバージョン印を更新できなくなり**、
ゲートの段階 0 が赤のままになる。singlefs で実測：12 件の「遅れ」はすべて
プロジェクト自身の編集だった（`.claude/kb/INDEX.md` は 36 行がプロジェクト独自の文面）。

プロジェクトは引き取ったファイルを `$ROOT/.claude/install-owned` に書く。
一行に一件、`<相対パス>  # 理由`。**理由は必須**：ファイルを引き取るということは、
以後そのファイルへの上流の変更が届かなくなるということであり、
理由の欄はそれを正面から引き受けさせるためにある。一覧は必ず出力に報告する——
黙って数件だけ比較を飛ばすのは、この防護が未実装なのと見分けがつかない。

赤になるのは二つ：理由が書かれていない場合と、`install.sh` が置きもしないパスを
書いた場合（その項目は何の効果も持たないのに、そのファイルが免除済みだと
人に思わせてしまう）。**壊れた一覧はその場で止める**。バージョン印はその先へ進まない。

`README.md` の「バージョン印は毎回更新される」という一文は誤りだったので併せて直した——
`install.sh` の実際の動作とはとうに食い違っていた。

変異は五件を個別に実行した。うち一件が盲点を露わにした：「壊れた一覧は止める」の一段を
丸ごと削っても、既存のサンプル二件は**揃って緑のまま**だった——それらは同時に内容も
遅れており、赤くしていたのはそちらの経路だったのである。他に遅れの無い場面を
用意して初めて切り分けられた。自己検査は 182 → 188 件。

## 0.0.32 — 2026-09-06

**gate-lint に三つ目の拒否形態を追加した：直に印字される `✗`。** これまでは `lib.sh` の
`bad` と `die` しか見ていなかったが、**プロジェクト固有の段階の多くは `lib.sh` を
source しない**——直に `echo "  ✗ …"` と書くか、判定を埋め込み python の
`print('  ✗ …')` に書く。この一群は一度も検査されていなかった。

singlefs のローカル段階で実測：この形の拒否が 46 箇所あり全て免除されていた。
検査を入れると **14 箇所は次の一手が一言も無い**（`15-research-build.sh` の「cargo が無い」、
`70-citations.sh` の「$S が見つからない」——どちらも何をすべきか言わずに `exit 1`）。
gate-lint が見る拒否の数は 125 → 170 になった。

判定の二箇所は実データに合わせて較正した：

- **窓は 5 行ではなく「次の拒否まで」。** python は先に `✗` を印字し、該当項目を繰り返しで
  並べ、最後に対処を書くことが多い。5 行ではこの一群をまとめて誤判定する
  （サンプル `printrejok` が見張る）。
- **heredoc 本体は既定ではコードとして読まないが、解釈器 heredoc は読む。**
  `python3 - <<'PY'` の中の `print` は実際に提出者へ表示される。従来どおり読み飛ばせば、
  python で書かれた段階は依然として一括で見えないままになる（サンプル `printrejpy` が見張る）。

二つの免除はどちらも明示的に書くこと。推測はしない：繰り返しの中の一件ずつの行には
`# gate-lint:detail`、要約行には `# gate-lint:summary`。

`rules/sop-first.md` の拒否形態表に三行目を追加した。サンプル四件、変異四件を個別に実行し、
それぞれちょうど一つの用例に捕まった。自己検査は 178 → 182 件。

⚠️ **この検査により singlefs のゲートは 14 箇所で赤になる。** すべて同プロジェクトの
`.claude/gate.d/` の中である。上流の三リポジトリには影響しない——共有パッケージには
直に印字される拒否が一つも無く、すべて `bad` を通っている。

## 0.0.31 — 2026-09-06

**新しい規則 `rules/pushback-discipline.md`：提案は誰が出したかによって検査を免れない。**
利用者が出した案も、他所から来た案と同じ関門を通る。実測データ、あるいは既に確認済みの
事実と食い違うときは、手を付ける**前に**三つを揃えて言う：食い違う相手はどれか（出典付き）、
提案どおりにすると何が起きるか（どんな観測でそれが分かるか）、第三の道があるか。
相手が明確に言い直したらそのまま実施し、もう説得しない。ただし**警告は必ず記録に残す**。
**やり終えてから食い違いに気づいた場合も同じく言い、必要なら丸ごと撤回する**——
費用は利用者が負い、コードは正しさに責任を負う。「ここまで作ったのだから」は続ける理由にならない。
コードの変更に取り消せないものは無い：撤回の代償はかつて人の時間だったが、今その部分は
機械の仕事であり、数万行と数百行に桁の違いは無い。本当に戻せない `git checkout`・
`rm -rf`・外部への恒久的な約束は `command-safety.md` が扱う。

**警告は `.claude/warnings/<日付>.md` に書く。** 日付ごとに一ファイル、警告ひとつにつき
`##` 節ひとつ、四項目すべて：提案 / 異議 / 既知のリスク / 結末。他の場所からは必ずここへ
張り、二部目は作らない（`kb-discipline.md` 第 4 条）。日付ごとに分けるのは
**古いファイルを直さずに済ませるため**である——警告は書いた時点でその日の記録になる。
`install.sh` がプロジェクト側にこのディレクトリを用意する。

**doc-lint に二つの検査を追加した**：

- `.claude/warnings/` 下のファイル名は `YYYY-MM-DD.md` であること、各 `##` 節に四項目が
  揃っていること。項目名は言語ごとに取り、三言語とも走る：固定の項目名を見るのであって
  字形の黒名簿で語義を当てているのではないため、「語彙表が無いので未実装」の段には入らない。
- `rules/*.md` と `CLAUDE.md` 中の `@rules/` 参照は一項ずつ一致していること。
  どちらの向きも無言で失敗する：参照されていない規則は文脈に入らないのに、
  効いている規則と見た目が変わらない。存在しないファイルへの参照は、
  その行が展開されないだけで、CLAUDE.md は完全に読めてしまう。
  この検査は今回自分が必要としたものである——規則を一つ足して CLAUDE.md に
  書き忘れても、これまでは何処も赤くならなかった。

サンプル七件、変異五件を個別に実行し、それぞれちょうど一つの用例に捕まった。
自己検査は 171 → 178 件。

## 0.0.30 — 2026-09-06

**en / ja 両リポジトリのゲートが赤かったのを直した。** 二箇所とも、
一組の規則を多言語リポジトリに複製したときに空いた穴である：

- `doc-lint` のサンプルは中国語で書かれているが、`scripts/` は各言語リポジトリへ
  バイト単位でそのまま複製される。その結果 en リポジトリでは、同じサンプルが
  `## Revision history` と突き合わされ、44 件が一斉に偽陽性になった——
  doc-lint 自体は一文字も壊れていない。言語は**サンプルの属性**であって
  リポジトリの属性ではない。`selftest` は `DOC_LINT_LANG=zh` でサンプルの言語を固定する。
  この口は判定基準を差し替えられるので、**設定したら必ず出力に出す**。
  その一行が残っているかを見張る自己検査も一件追加した。
  0.0.25 で判定基準を言語別にしたときに空いた穴で、en / ja はそれ以来ずっと赤だった。
- en リポジトリは `templates/kb/` の D1 と E1 の略称の雛形をどちらも
  `<name, 24 characters or fewer>` と訳しており、「一つの略称を二つの採番で共有してはならない」
  に抵触していた。それぞれに名詞を付けた。zh / ja は元から別々の訳になっている。

**doc-lint に除外一覧を追加した：そのまま保存した証拠は後から書き換えてはならない。**
`research/prompts/` のようなディレクトリには、モデルに渡したプロンプトがそのまま入っている。
産物と一対一で対応しており、一文字直せば産物はもうその入力に対応しない。
プロジェクトは `.claude/doc-lint-exclude` で宣言する。一行に一件、**それぞれ理由を書くこと**。
存在しないディレクトリを指す項目、一件も除外できていない項目は赤とする——
効いていない除外項目は、そのファイル群がもう避けられていると人に思わせてしまう。
除外は必ず出力に報告する：黙って二百件を読み飛ばすのは、この検査が未実装なのと見分けがつかない。
規則は `rules/evidence-discipline.md`。サンプルは七件で、除外宣言を持たない対照が一件含まれる。

## 0.0.29 — 2026-09-06

**「已被 X 覆盖」の判定が、この SOP が管理する採番だけに反応するようになった**
（`D` 決定 / `E` 実験 / `C` 未済チェック / `I` 不変条件 / `A` 前提 / `O` オラクル）。
実測した誤検知：ディスク上のバイトについて述べた一文「U1 を再構築するには U2 の元の
バイトが要る——それは既に U9 に上書きされている」が、「本文で『X に上書きされた』と
その場に注記してはならない」に当たると判定された。この文の「覆盖」はバイトが書き潰される
ことであって、条項が覆されることではない。二つの語義が同じ語に重なっており、
両者を分けられるのは「上書きした側が採番された条項かどうか」だけである。

## 0.0.28 — 2026-09-05

**件数の判定が python の f-string も認めるようになった。** shell の `$n` しか
認めないと、python で書かれた段階が軒並み偽陽性になる——成功の一文が python 側で
整形されるためである（singlefs の二つの段階で実測）。サンプルは `countpy`。
`nocount` は依然として赤で、判定基準は骨抜きになっていない。

## 0.0.27 — 2026-09-05

**0.0.26 が自ら持ち込んだ無限ループの修正と、selftest への時間切れ保護。**

- `gate-lint` が算術展開を取り除く際、対になる `))` は `$((` の**後ろ**だけで探す。
  行全体から最初の `))` を探すと止まらなくなる：`if ((okc)); then …; pass=$((pass+1)); fi`
  のような行では最初の `))` が `$((` より前にあり、切った後の文字列も `$((` を含んだまま
  短くならない。実際の資料で即座に踏んだ——9 分走っても終わらず、赤でも緑でもなかった。
  サンプル `arith2` は最後まで走り切り、かつ緑でなければならない。
- `selftest` のサンプル実行箇所二つに `timeout` を入れた（既定 60 秒、`SELFTEST_TIMEOUT` で変更可）。
  **固まった検査は赤い検査より危険である**：ゲートの出力の中では何物でもなく、ただ戻ってこない。

## 0.0.26 — 2026-09-05

**プロジェクト固有の段階が初めてゲート自身の管理下に入り、singlefs の三日分の作業から
剥がした三つの規律を追加した。**

**一、`.claude/gate.d/` を二つの lint の走査対象に入れた**

`gate.sh` がプロジェクト固有の段階ディレクトリを `gate-lint` と `shell-lint` に渡す。
従来はどちらの走査範囲にも入っていなかった——それらのスクリプトも共有段階と同じように
提出者を拒否するのに、一度も検査されていなかった：最初に通した時点で出口の無い拒否が
7 件出た（singlefs 実測）。

**二、gate-lint に三つ目の検査：成功の一文に何件検査したかを出す**

「0 件走査」は通過ではない。判定基準の走査範囲を少し狭く書くと、対象が全部その最初の
一歩で飛ばされ、通過でも失敗でもなく末尾は緑を報告し続ける（singlefs の C114：ある段階の
第 3 項がまさにその状態で緑だった）。**成功を報告している**スクリプトだけを判定し、
本当に対象を走査しないものは `# gate-lint:nocount <理由>` と書く。
サンプルは `nocount` / `countok`。本パッケージ自身の `show-me-test.sh` が最初に咬まれた。

**三、gate-lint の偽陽性を一つ修正**：`bad=$((bad + 1))` の `bad ` は `(` の直後にあり、
CMD_POS では一つの拒否と読めてしまう。判定前に算術展開を取り除く。サンプルは `arith`。

**四、規則の追補**（いずれも実測の代価つき）

- `evidence-discipline`：**産物を引くときは行ごと写す**（同じ差分が四ラウンド連続で誤伝）、
  **新しい判定基準はその場で既存項目に振り直す**（物差しを新参にだけ振っていた）、
  標本の表に一行（「同じ産物の中に反例が無いか」）、藁人形の節に
  「ある腕の数値を引くときは定義も一緒に」。
- `show-me-test`：赤くなることを示す条は**設計上の論証にも効く**——
  必ず非ゼロを報告する世界を先に作ってからルールを直す。
- `skills/decide` 硬要件 6：**人が裁定した一文を動かしたら `checks-owed.md` に
  未返済の一件を記す**。

## 0.0.25 — 2026-09-02

**訳文リポジトリに残っていた最後の中国語を一掃し、`agents/` の層を立てた。**
どちらも「注意書き」ではなく、赤くなる検査として入れた。

**一、skills / templates を「原様複製」から「一篇ずつ翻訳」へ**

これまでは `i18n-sync` の `SHARED` にあってバイト単位で複製されていた。その結果、
本リポジトリの skill 本文・プロジェクト骨格・litmus のコメントはすべて中国語であり、
しかも `templates/CLAUDE.project.md` は `install.sh` が利用者のプロジェクト直下の
`CLAUDE.md` として書き出すものだった。「日本語で作業する人が中国語を読む必要はない」と
真っ向から矛盾する。

- マニフェストは 14 条から 28 条へ：`CLAUDE.md`、`rules/`、`agents/`、`skills/`、
  `templates/`（`*.litmus` を含む）。判定基準は一つ、「人が読む散文が含まれているか」。
- **網羅性の検査を新設**：どちらの一覧にも入らない文書はその場で赤になる。
  これは「空白」の機械判定可能な形である——`.md` を足して翻訳の要否を決め忘れても、
  ゲートが思い出させる。
- **来歴マーカーの位置と注釈形式をファイル種別で決める**：`SKILL.md` の YAML
  frontmatter は第 1 行から始まらなければならない（その上に置くと skill は黙って
  入らなくなる）。`.litmus` の第 1 行は `C <名前>` でなければならず、さもないと
  herd7 が `splitter error in sublexer first line` を出す（herd7 を実際に走らせて確認）。
  押印後に読み戻して確認し、両方の落とし穴を赤くなる検査にした。
- **`install.sh` はプロジェクトへ配置する際に来歴マーカーを剥がす**：それは配布層の
  帳簿であり、利用者のプロジェクトへ写せば決して更新されない古い注記になる。しかも
  相手がこれから編集するファイルに貼り付く（実際にインストールして判明）。
- 訳文 28 篇（en / ja 各 14 篇）を書き直した。三言語の litmus を herd7 で実走させ、
  判定は一致。

**二、`agents/` を統治下に入れる**

`agents/INDEX.md` に、何を置くか（`skills/` との境界：読むものか、委譲するものか）、
書き方、三つの規律、つなぎ方を記した。マニフェストに入れて一篇ずつ翻訳し、`GOVERNED`
に入れ、`install.sh` は agent スタブを配置する。空は状態なので口に出す：ディレクトリが
空のあいだ `manifest.sh` は「統治下にあり現在は空」と報告し、黙って通過させない。
doc-lint も、agent 定義に履歴節を残さないこと、`name`（ファイル名と一致）と
`description` を持つことを要求するようになった——どちらも欠けてもエラーにならず、
ただ静かに効かなくなるからである。

**三、ついでに直したもの** — いずれも自己検査に標本を食わせて露出した

- `manifest.sh` の `gen()` がパイプラインの中で `cd` を落としていた。リポジトリ直下から
  走らせると cwd がたまたまパッケージ直下と一致するので、表に出なかった。
- 存在しないディレクトリに対する `find` が `translated_paths` 全体を失敗させていた。
  `2>/dev/null` はメッセージを隠すだけで終了コードは隠さない。`--update` は終了コード
  1 で、しかも一文字も出力しなかった。
- `stamp_read` は sed を使っていたが、BRE の `\|` が区切り文字と衝突する。黙って何にも
  一致せず、押印後の読み戻し断言でようやく捕まった。awk に変えた。

判別力：用例 111 → 120。新しい検査はすべてミューテーションで検証した——変更前は緑、
変更後は赤。うち五つは今回の作業自身が作った盲点（網羅性の検査、第 1 行が押し下げられる、
install での剥がし、agent の `name`、`GOVERNED` の `agents/`）であり、標本を用意して初めて
噛むようになった。

作業上の記録を一つ：今回「マニフェストだけ写して訳していない」が二度起きたが、どちらも
自分の手によるものである。一括編集スクリプトが途中で止まり、片方の言語の編集は走らず、
それでも両方に押印され、ゲートは全緑だった。来歴マーカーが示せるのは「誰かが訳したと
主張している」ことだけで、「実際に訳した」ことではない。内容を一字ずつ突き合わせて
初めて分かった。

未着手：GLOSSARY の「説明」列は依然として参照リポジトリの言語である——本パッケージに
残る最後の言語の欠落であり、当該ファイル冒頭に明示して記録してある。

## 0.0.24 — 2026-09-02

**この版は単独では公開していない。0.0.25 と同じコミットで出る。**
下流から見るとバージョンは 0.0.23 から 0.0.25 へ飛ぶ。0.0.24 を探さないよう、ここに記す。

ミューテーションテストによる第二回の敵対的監査とその一括修正。**主題は判別力**：
前回追加したゲート検査のうち六条は、まるごと削除しても `selftest.sh` が
54/54 で緑のままだった——検査はあるが、検査を見張るものがなかった。

- **判別力**：doc-lint の履歴記述パターン群 / `CLAUDE.md` の履歴節 / kb の履歴節、
  gate-lint の `howto` 窓幅、lkmm の静的検査二条、show-me-test の注釈の広さに、
  それぞれ単一目的の標本を用意。`blind` の `want` は六条の検査が共有する
  「本文では禁止」だったため、検査ごとに分割した。八つの変異はすべて赤になる。
  用例 54 → 69。
- **doc-lint の死んだパターン**：`'[〔【\[]已废弃[〕】\]]'` は GNU grep では決して
  一致しない——POSIX では角括弧集合の中の逆斜線は字義通りだからである。修正した。
- **`die` はもう免除されない**：`die` は `bad` + `exit`、すなわち拒否である。
  `lib.sh` の `die` を「メッセージ + 対処」の二段にし、gate-lint は一文しか持たない
  `die` を赤とする。17 箇所の呼び出しに対処を補った。
- **`shell-lint.sh` を新設**：command-safety のうち機械判定できる二条を赤くなる検査に——
  パターン一致でプロセスを殺すこと、サブシェル内の代入で値を外に持ち出すこと。
- **`qemu/run.sh` の `$QEMU_LOG`**：常に `$( )` の中で走る関数の内側でしか代入されず、
  親側の六箇所の参照は `set -u` の下で `unbound variable` だった——
  **五つの失敗分岐が `howto` を出す前に落ちていた**。作業ディレクトリは呼び出し側が持つ。
  これは command-safety 自身が記している落とし穴そのものである。
- **gate スキルの「赤くなることを示す」例が実測で緑だった**：`>>` は「改訂履歴」の
  見出しより後ろに追記され、本文走査はそこで止まる。差し替えた。
- **規範本体の定義は一箇所**：`version-discipline.sh` の `GOVERNED` を正とし、
  `CLAUDE.md`・README・session-wrapup がそれぞれ別の一覧を持っていたのをリンクに変えた。
  `README.md` と `I18N` を管理下に入れた。
- 重複と腐り：`CLAUDE.md` と README で重複し既にずれていた多言語の段落を README 一箇所に。
  インストーラは三つではなく六つのものを置く（プロジェクト直下の `litmus/` を含む）。
  gate スキルの段階表を 4 行から 9 行に。「三言語」と書き固めた 15 箇所を
  `I18N` の `languages=` に従う言い方へ。
- `gate.sh`：「上流より古いかは未確認」を集約の「今回走らせていない段階」に出す——
  従来は冒頭で一度警告するだけで、集約は「N 段階すべて通過」と報告していた。
- 規則の追補：sop-first の対処要求が `die` を含むようになった。
  design-doc-discipline は検査コードの落とし穴コメントを例外とする。
  kb-discipline は `INDEX.md` に履歴節を置かないと明記。
  command-safety はどの二条がすでに検査かを示す。
- README を三節から一節に統合。gate スキルの「よくある偽の失敗」表から、
  もう存在しないメッセージ文を差し替えた。doc-lint の死んだパターンは
  `'[[〔【]已废弃[]〕】]'` に修正した。
- 復核後の追加：英語版 `show-me-test.md` で否定が落ちていた
  （「ゲートは誰も正しさに到達しないことを保証する」になっていた）。
  gate-lint と shell-lint がパッケージ直下も走査するようになり、
  `install.sh` の対処のない `die` 四箇所を検出して修正。
  `howto` の窓は `bad` 自身を含めて 5 行であって 4 行ではない。

## 0.0.23 — 2026-09-02

敵対的監査を受けたゲート修正の一括投入（監査レポートは当該セッションの記録）：

- doc-lint：閉じていないコードフェンスを赤に；「履歴節」の後の `##` 節を赤に；
  履歴節内の表による不変条件定義は無効に；rules ファイルの履歴節を赤に；
  rule-definition マーカーは CLAUDE.md / rules / skills に限定；登録済み番号の
  not-numbers 免除を赤に；「脚本文件」「同上游」等の複合語を誤検知しないよう修正。
- gate-lint：`bad` を全形態で認識（単/二重引用符、変数、`;{|&` / `then` / `else`
  の後）；コメント内の howto は対処と見なさない；サマリ免除を
  「失敗/未通過/未過：$カウンタ」に限定。
- Show me test を show-me-test.sh に分離：コメント内の `#[test]` は無効；
  tests/ は `.rs` のみ有効；build.rs もコード扱い；デフォルトブランチでは
  diff 基準を HEAD~1 に後退させ、先にコミットしても「判定対象なし」にならない。
- lkmm：各 Never litmus に同名 `-nofence` の対照ペアを必須化（全体で
  Sometimes が 1 本あれば良い、を廃止）；静的検査を herd7 探索より前に；
  全拒否に howto を追加。
- i18n-sync / manifest：失敗分岐の diff 診断パイプラインが set -e + pipefail で
  スクリプトを途中終了させていた（howto と残り言語の検査が消える）——
  `|| true` で保護；i18n-sync は先にマニフェストの鮮度を検証；GLOSSARY.md を
  共有（そのまま複製）セットに追加。
- version-discipline.sh を新設：規範本体を変えて VERSION を上げなければ赤
  （従来は注意書きのみ）。
- selftest を全ゲートスクリプトに一般化：doc-lint / gate-lint / lkmm の
  サンプルに加え、show-me-test / version-discipline / manifest / i18n-sync の
  スクリプト化ケース、計 54 件。

## 0.0.22 — 2026-09-02

- session-wrapup に「同じリポジトリで他のセッションが飛んでいないか」節を追加；
  show-me-test にミューテーションリスト；test-discipline に「決定的モデル × N 回」
  「ミューテーションテストが証明するのはアサーションが赤になれること」；
  GLOSSARY にミューテーションテスト関連の用語を追加。

## 0.0.21 以前

`git log --oneline` を参照——各コミットメッセージは「バージョン：何を変えたか」
の形式で書かれている。
